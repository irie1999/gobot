# 日本株・同日決済の仕組みを考える

現象と制約だけを書いたものです。**成績・検証手順・これまで試した案は
一切含みません**（先入観なしで考えてもらうため）。

---

## 問題

次の条件で、日中の値動きから利益を取る仕組みを設計してください。

- 対象は日本株（東証、約1,500銘柄。株価 1,000〜6,000円）
- **持ち越さない**。その日のうちに必ず建てて、その日のうちに閉じる
- 資金 400万円
- 売買は証券会社の API 経由。**板をリアルタイムで見られるのは同時に50銘柄まで**
- 空売りも買いもできる（信用取引）

---

## 分かっていること

**単に日中に売る／買うだけでは何も出ません。** 寄りで建てて引けで閉じるのを
全銘柄で機械的にやっても、平均はゼロです。上げ方向にも下げ方向にも偏りはありません。

一方で、条件を付けると偏りが現れます。いま使っているのは次の性質です。

> **前日に大きく動いた銘柄が、翌朝さらに同じ方向へギャップして寄ると、
> その日のうちに戻る。**

上げ側（大きく上げた翌朝さらに上へ飛ぶ → その日は下げる）と
下げ側（大きく下げた翌朝さらに下へ飛ぶ → その日は上げる）の両方に出ます。
過剰反応の巻き戻し、という説明が自然です。

---

## いまのルール（叩き台。これを使う必要はありません）

### 上げ側 — 空売り

| | |
|---|---|
| 前夜 | 前日リターン（終値 ÷ 前々日終値 − 1）が **+1.75% 以上** |
| 09:00 | 始値が前日終値の **+1.0% 以上**なら建てる |
| 建て方 | 始値で空売り。100株 |
| 決済 | その日の**引け**。損切りも利確も置かない |

### 下げ側 — 買い

符号をすべて反転。前日 **−1.75% 以下** → 始値が **−1.0% 以下** → 買う → 引けで売る。

条件を満たす銘柄は 1日 25件前後。資金400万で建てられるのは 1日十数件までです。

---

## 制約（設計に効くもの）

### 時間

東証は 09:00 に寄り付き、15:30 に引けます。11:30〜12:30 は昼休みで取引がありません。

**寄りちょうどで建てられるかが大きく効きます。** 09:00 から遅れるほど不利になり、
5分遅れると取れる幅の大半が消えます。したがって 09:00 に重い計算をする設計は成立しません。
前夜のうちに候補を確定させておく必要があります。

### 銘柄数

板を同時に購読できるのが50銘柄なので、前夜の候補（1日100件以上ある）を
50件まで絞ってから翌朝を迎えることになります。この上限は証券会社側の仕様です。

### コスト

- 手数料は **0**（大口優遇プラン）
- 呼値（価格の最小刻み）は 3,000円未満で1円、3,000〜5,000円で5円
- 引けの成行注文は板の厚みぶん滑ります
- **実際の執行でどれだけコストがかかるかは、まだ測っていません**

### その他

- 同日決済なので、夜間のギャップ・配当落ち・貸株コストは無関係です
- ごく稀に、ストップ高（値幅制限の上限）に張り付いて引けで買い戻せない日があります。
  空売り側だけで、月1件に届かない頻度です

---

## 使えるデータ

| | 期間 | 備考 |
|---|---|---|
| 日足（始値・高値・安値・終値・出来高） | 2007年〜 | 株式分割を遡及調整済み |
| 5分足・1分足 | **直近2年のみ**（最古 2024-07） | ローリング。それ以前は入手不可。分割は**未調整** |
| 板・気配・歩み値の履歴 | **無し** | 過去に遡って板を見ることはできません |

日足では「その日の高値と安値のどちらが先に付いたか」が分かりません。
同一日内で価格に触れる順序が必要な検証は、直近2年の分足でしか行えません。

日経平均・TOPIX・米国指数・為替・VIX などの日足も取得できます。
日経225先物は日次のみで、分足は入手できません。

---

## コードを書くときの実務メモ

### 置き場所と形式

| | 場所 | 形式 |
|---|---|---|
| 5分足 | 環境変数 `MINUTE_5M_DIR`、無ければ `data/minute_5m` → 隣接する `stock_5min` を自動検出 | `<5桁コード>.pkl`。pickle 化した DataFrame |
| 1分足 | 環境変数 `MINUTE_1M_DIR`、無ければ `~/.jquants_cache/minute` | `<5桁コード>_1m.pkl` |
| 日足 | `.rsi2_cache/`（yfinance の永続キャッシュ） | `<7203_T>.pkl` |

**5分足と1分足は解決の仕組みが別**です（環境変数も置き場所も違う）。混同しないこと。

### 既存のローダー

```python
from daytrade_data import (load_intraday,          # 分足（1銘柄）
                           load_intraday_batch,    # 分足（まとめて）
                           available_local_symbols,
                           split_by_day)           # 日ごとに切り分ける
from backtest_limit_entry import fetch             # 日足（キャッシュ優先）
```

`daytrade_data.normalize_minute_df()` を通すと、どのソースでも
**index = DatetimeIndex（tz-naive / JST）、列 = open, high, low, close, volume（小文字）**
に揃います。自前で読むよりこれを通すほうが安全です。

### ⛔ 銘柄コードが2種類あります

分足のファイル名は **J-Quants の5桁コード（4桁 + 末尾0）**、日足と yfinance は
**4桁 + `.T`** です。

```
7203.T  ←→  72030        # daytrade_data.yf_to_jquants() が変換
```

`available_local_symbols()` は **5桁のほう**を返します。これをそのまま
yfinance に渡すと全銘柄が「上場廃止」扱いで失敗します。

### ⛔⛔ 5分足は株式分割が未調整です

日足は分割を**遡及調整**しますが、5分足は**保存したときのまま**です。
そのため分割した銘柄では、分割前の日に「5分足が日足の2〜15倍」という状態になります。
値幅制限があるので現実には起こり得ない値動きに見えます。

対策は、**同じ日の5分足の終値の中央値と日足の終値を突き合わせ、30%以上ずれていたら
その銘柄日を捨てる**ことです。実測では 166,962銘柄日 のうち **2,311日（1.38%）/ 30銘柄**が
該当し、比率はすべて 2倍・3倍・4倍・5倍・10倍のような整数倍でした
（各銘柄の汚染は必ず分割の権利落ち日で終わります）。

これを掛けずに5分足で含み損を計算すると、資金400万に対して
「−1億9,500万」のような値が普通に出ます。

### その他の落とし穴

- **先頭バーが 09:00 とは限りません。** 09:05 始まりの銘柄日がかなりの割合であります。
  「寄りの1本目」を前提にすると、その日が丸ごと抜けたり判定がずれたりします
- **11:30〜12:30 は昼休み**でバーがありません。等間隔を仮定しないこと
- **yfinance の分足は60日まで**しか遡れません。履歴の検証には使えません
- 日足の `fetch()` は `min_start_date` を渡さないと、キャッシュの**開始日を見ません**
  （見るのは最新バーの日付だけ）。長い期間が要るときは必ず渡すこと

### 参考になる既存の定数・テーブル

| | 場所 |
|---|---|
| 銘柄ユニバース（東証プライム約1,800） | `symbols_listed_prime.py`（`fetch_listed_symbols.py` が生成） |
| 空売りできない銘柄 | `not_shortable.py`（`check_shortable.py` が証券会社に照会して生成。リポジトリには入っていません） |
| 東証の制限値幅テーブル | `check_price_limit._TSE_LIMIT_TABLE` |
| 呼値（価格の最小刻み） | `backtest_limit_entry.tick_size()` |

---

## 求めるもの

上の現象を使うか使わないかは自由です。制約の中で成立する仕組みの案と、
それが本物かどうかをどう確かめるかを教えてください。
