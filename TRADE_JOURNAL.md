# 手動デイトレ 記録・分析ノート

自分の手でやったトレードを1件ずつ `trades.csv` に貯めて、**何が勝ちトレードを
作っているか**を後から見つけるためのノート。

> このノートはアルゴリズム検証とは無関係。**手でやるトレードだけ**を扱う。

---

## 1. 記録のしかた（あなたがやること）

**チャットに1行投げるだけ。CSV への記帳は私がやります。**

### 書式（順番だけ守れば表記はゆるくてOK）

```
8/25 練習 9984 売 09:12 5469 損切5500 → 09:31 5420 利確 100株 ○ 高値切下げ 地合い弱 自信あり
```

読み方:
```
日付 モード 銘柄 方向 建時刻 建値 損切値 → 決済時刻 決済値 決済理由 数量 ○× 根拠 地合い 気持ち
```

**思い出せない項目は空でいい。** 埋まらないより、雑でも出す方が価値があります。
まとめて何件か投げてもらっても構いません。

---

## 2. 項目の定義

### 必ず埋めたいもの

| 項目 | 中身 |
|---|---|
| `date` | 取引日（リプレイなら**再生していた日付**） |
| `mode` | **`練習`**(リプレイ) / **`デモ`**(リアルタイム紙トレード) / **`実弾`** |
| `symbol` | 銘柄コード |
| `side` | `買` / `売` |
| `qty` | 株数 |
| `time_in` / `entry` | 建てた時刻と値段 |
| **`stop`** | **建てる前に決めた損切り値** ← 建てた後に決めた場合はその旨をメモに |
| `time_out` / `exit` | 決済した時刻と値段 |
| `exit_reason` | `利確` / `損切` / `手動` / `引け` |
| **`rule_ok`** | **決めた損切り値で切れたか。`○` / `×`** ← 最重要 |
| `setup` | **入った根拠を短く1つ**（例: 高値切下げ / 前日終値割れ / 寄り天 / VWAP戻り） |

### 埋まれば埋めたいもの

| 項目 | 中身 |
|---|---|
| `target` | 決めていた利確値 |
| `market` | その日の地合い（`強` / `弱` / `横`） |
| `confidence` | 建てたときの気持ち（`自信あり` / `なんとなく` / `焦り` / `取り返し`） |
| `note` | 一言。後で読んで思い出せる程度 |

### 私が自動で計算するもの

`損益` / `R倍数`（= 損益 ÷ 損切り幅）/ `保有分数` / `時間帯`

---

## 3. なぜこの項目なのか

- **`rule_ok`（○×）が最重要。** 決めた損切りを守れていないトレードが混ざっていると、
  他のどの数字も意味を持たなくなる。まずここを100%にする
- **`setup` は1つに絞る。** 複数の理由で入ると、後で「何が効いたのか」が分からない
- **`confidence` は効く。** 「なんとなく」「取り返し」で入ったトレードの成績を
  切り出すと、たいてい大きくマイナスになる。減らすべき行動が特定できる
- **`mode` で分ける。** 練習・デモ・実弾を混ぜて集計すると何も分からない

---

## 4. 分析の進め方（私がやること）

### 段階を守る。飛ばすと誤読する

| 件数 | 見るもの |
|---|---|
| **〜30件** | **`rule_ok` の○×だけ。**勝率もPFも見ない（サンプルが少なすぎて意味が無い） |
| **30〜50件** | 全体の勝率・平均R・期待値。`setup` 別の件数分布 |
| **50件〜** | `setup` 別 / 時間帯別 / `confidence` 別 のクロス集計。**勝ちトレードの共通項を言語化** |
| **100件〜** | 期待値マイナスの `setup` を捨てる。プラスのものに回数を寄せる |

⚠ **少ない件数でパターンを見つけると、ほぼ確実に誤読します。**
10件で「この型は勝率70%」と言っても、それは偶然と区別できません。
最初の30件は**成績を見ずに、損切りを守ることだけ**に集中してください。

### 出す集計

- 全体: 件数 / 勝率 / 平均R / 期待値R / 合計損益 / 平均保有分数
- **`rule_ok` 別**: 守ったトレード vs 破ったトレードの損益差
- `setup` 別 / 時間帯別 / `market` 別 / `confidence` 別
- **勝ちトレードだけを抜き出して、共通点を洗い出す**

---

## 5. 運用

1. 練習・デモ・実弾のどれでも、トレードしたらチャットに1行投げる
2. 私が `trades.csv` に追記して commit する
3. 節目（30件 / 50件 / 100件）で私から集計を出す
4. 「この `setup` を捨てる / 増やす」をあなたが決める

いつでも「いま何件？」「集計出して」と言ってもらえれば出します。
