# bot 自動起動セットアップ

毎朝 8:55 に自動起動するための設定手順。

## 前提条件

- ✅ kabu STATION (Windowsアプリ) インストール済み
- ✅ APIパスワード設定済み
- ✅ `.env` 設定済み
- ✅ kabu STATION が常時起動 (or 起動スクリプト併設)

## PC環境の準備

### スリープ無効化 (重要)
取引時間中にPCがスリープすると bot 停止します。

#### 方法1: 平日のみスリープ無効
1. `設定` → `システム` → `電源とバッテリー` (Win11)
2. `画面とスリープ`
3. 「**電源接続時、次の時間が経過するとデバイスをスリープ状態にする**」を **「なし」**

#### 方法2: タスクスケジューラで定時に Wake (上級)
PC が休止状態でも起動可能だが、設定が複雑。

### Windowsログイン自動化 (推奨)
PCを再起動した後、ログインしないと bot は動かない。
- **自動ログイン設定**を有効化 (netplwiz)
- ただしセキュリティリスクあり、取引PC専用機推奨

## kabu STATION 起動の自動化

kabu STATION は手動ログインが必要なため、完全自動化は困難。
**毎朝起動するか、24時間起動したまま**が現実的。

### 推奨運用
```
[週末・夜間]    PC起動したまま、kabu STATIONも起動したまま
[平日 8:55]    Task Schedulerが自動でbotを起動
[平日 15:00]    botが自動終了 (ログ保存)
```

## Task Scheduler 設定手順

### Step 1: タスクスケジューラを開く
- `Win + R` → `taskschd.msc` → Enter
- または `スタート` → `タスクスケジューラ` で検索

### Step 2: 基本タスクの作成

1. 右ペイン「**基本タスクの作成**」
2. **名前**: `kabu_donchian_dryrun`
3. **説明**: `Donchian bot ドライラン (毎朝9:00)`

### Step 3: トリガー設定

1. **毎週**を選択
2. **開始時刻**: `08:55:00`
3. **曜日**: 月・火・水・木・金 にチェック (土日は除外)

### Step 4: 操作設定

1. **プログラムの開始**
2. **プログラム/スクリプト**: 以下を入力
   ```
   C:\Users\to732\OneDrive\ドキュメント\kabu station\gobot\run_dryrun.bat
   ```
3. **開始 (オプション)**: 以下を入力
   ```
   C:\Users\to732\OneDrive\ドキュメント\kabu station\gobot
   ```

### Step 5: 完了 + 詳細設定

1. 「**完了時にこのタスクのプロパティダイアログを開く**」をチェック
2. **完了**

### Step 6: プロパティの追加設定

#### 「全般」タブ
- ☑ **ユーザーがログオンしているときのみ実行する**
- ☑ **最上位の特権で実行する** (任意)

#### 「条件」タブ
- ☐ **コンピューターをAC電源で使用している場合のみタスクを開始する**
  - チェックを**外す** (ノートPCで動かす場合)
- ☑ **タスクを実行するためにスリープを解除する**

#### 「設定」タブ
- ☑ **タスクを要求時に実行する**
- ☑ **タスクが既に実行中の場合**: `新しいインスタンスを開始しない`

### Step 7: 動作テスト

1. タスクを右クリック → **実行**
2. ターミナルが起動して bot が動くか確認
3. `logs\YYYY-MM-DD_dryrun.log` に出力されるか確認

## 動作確認 (手動テスト)

タスクスケジューラに頼らず手動で:
```
cd "C:\Users\to732\OneDrive\ドキュメント\kabu station\gobot"
.\run_dryrun.bat
```

## ログの確認方法

```
type logs\YYYY-MM-DD_dryrun.log
```

または:
```
dir logs
```

## 本番運用への切替

ドライランで5営業日以上の動作確認が完了したら:

1. タスクスケジューラのタスクを編集
2. 「操作」を `run_live.bat` に変更 (`run_dryrun.bat` から)
3. ⚠️ **これで実発注**になる

または別タスクとして `kabu_donchian_live` を作成し、ドライランタスクを無効化。

## 緊急停止方法

### bot プロセスを kill
```powershell
# python.exe 全部止める (注意: 他のPythonも止まる)
taskkill /F /IM python.exe

# 特定のbotだけ止める (上級)
Get-Process python | Where-Object {$_.MainWindowTitle -like "*kabu_donchian*"} | Stop-Process
```

### kabu STATION で手動決済
1. アプリの「保有建玉」
2. 該当銘柄を「返済売り」

## トラブルシューティング

### Task Scheduler でタスクが起動しない
- ☑ 「ユーザーがログオンしているときのみ実行する」を確認
- ☑ Windowsアカウントに**パスワードが設定**されている
- ☑ 「最上位の特権で実行する」をチェック

### bot は動くがエラーで止まる
- `logs\YYYY-MM-DD_dryrun.log` を確認
- kabu STATION が起動中か確認
- `.env` が正しいか確認

### ログファイルが文字化け
- メモ帳で開く時に文字コードを「**UTF-8**」に
- またはVSCode、サクラエディタなどで開く

## 推奨: 監視通知の追加 (上級)

LINE Notify や Discord Webhook で:
- 起動時通知
- シグナル発生時通知
- エラー時通知
- 日次レポート通知

これにより外出中でも状況確認可能。
別途実装が必要 (要望あれば対応)。

## まとめ

| 項目 | 設定 |
|---|---|
| トリガー | 毎週 月-金 8:55 |
| 起動スクリプト | run_dryrun.bat (動作確認後 run_live.bat) |
| ログ出力 | logs\YYYY-MM-DD_*.log |
| 自動終了 | bot が15:00で自動終了 |
| PC状態 | 起動中 + ログイン中 |
| kabu STATION | 起動中 + ログイン中 |

## 自動化レベル別おすすめ

### Lv1: 半手動 (慣れるまで)
- 毎朝 8:55 に手動で `run_dryrun.bat` ダブルクリック
- ログ確認も手動

### Lv2: 半自動 (推奨)
- Task Schedulerで毎日自動起動
- 終了は bot の 15:00自動終了に任せる
- ログを毎晩確認

### Lv3: 完全自動 (上級)
- Task Scheduler 起動
- 通知連携 (Line/Discord)
- ログ自動集計
- 日次レポートメール送信
