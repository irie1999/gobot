# ⛔ このディレクトリのファイルは **検算専用のダミー** です

本物のモジュールと**同じ名前を、リポジトリ直下に置かないこと**。
2026-08-27 に `preopen_market.py` をダミーで上書きしてコミットし
(`8fdd34a`)、`--sweep-market` が **乱数で 40通りを掃いて p=0.280** という
無意味な結果を出しました。

検算で本物を差し替えたくなったら:
  1. このディレクトリに置く
  2. `PYTHONPATH=_dummy_for_container python ...` で読ませる
  3. **絶対に `git add` でリポジトリ直下に混ぜない**
