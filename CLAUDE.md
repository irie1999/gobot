# gobot 逆指値シグナル運用メモ

このドキュメントは Claude Code が gobot リポジトリを扱う際の前提知識です。
主軸は **`run_signals.py` (逆指値シグナル統合レポート)** の運用・改修です。
今後の修正は原則このファイルを起点に考えてください。

---

## 1. エントリーポイント

**`run_signals.py`** = 日々の運用コマンド。以下を1コマンドで実行します。

```
python run_signals.py                    # 全期間(365日) HTMLレポート
python run_signals.py --days 90          # 直近90日
python run_signals.py --date 2026-04-08  # 任意日シグナル確認
python run_signals.py --signal-only      # HTML生成せずシグナルだけ表示
python run_signals.py --no-browser       # HTML生成のみ（ブラウザ自動起動なし）
python run_signals.py --workers 8        # 並列数
```

内部で `check_signals_stop` (逆指値B: MACD/A7/RSI2) と `check_signals_breakout`
(ブレイクアウト: DON/VOL/MOM) の2グループを **ThreadPoolExecutor で並列実行**
(`run_signals.py:142-146`) し、タブ付き統合HTMLを `signals_combined_<date>.html`
に出力します。シグナル行は両グループ横断で `calc_recommend_score` 降順ソート
(`run_signals.py:154-164`)。

---

## 2. 依存ツリー

```
run_signals.py
├── check_signals_stop.py      (逆指値B  MACD / A7 / RSI2)
│     └── backtest_limit_entry.run_limit_backtest(entry_type="stop")
├── check_signals_breakout.py  (ブレイクアウト DON / VOL / MOM)
│     ├── scan_breakout_entry.calc_donchian / calc_vol_breakout / calc_momentum
│     └── backtest_limit_entry.run_limit_backtest(entry_type="stop")
└── backtest_limit_entry.py    (共通バックテストエンジン)
```

`run_limit_backtest` は **唯一の逆指値約定ロジック**。分岐は
`backtest_limit_entry.py:339-340` と `:433-438`。ここを触ると全スクリプトに影響します。

関連スキャン (普段は走らせない):
- `scan_entry_compare.py` — 1800銘柄を A(指値) / B(逆指値) / C(逆指値+ATR×0.1) 3パターンで比較
- `scan_breakout_entry.py` — ブレイクアウト3戦略のユニバーススキャン
- `compare_entry_mult.py` — 24銘柄で entry_atr_mult を細かく比較

---

## 3. 逆指値の数式と意味

シグナル前日終値 `close_prev` と前日ATR `atr_prev` から注文価格を計算します。

```
逆指値注文価格  order_p = close_prev + atr_prev * entry_atr_mult
損切り価格      stop    = order_p   - atr_prev * stop_atr_mult
目標価格        target  = order_p   + atr_prev * target_atr_mult
```

- `entry_atr_mult = 0.0` → **終値ちょうど** を逆指値に設定（= 翌日 高値 ≥ 前日終値 で約定）
- `entry_type="stop"` 時の約定条件: `hi >= order_p` (`backtest_limit_entry.py:339`)
- `entry_type="limit"` 時の約定条件: `lo <= order_p` (指値＝押し目買い、逆指値版と対比用)

定義箇所:
- `backtest_limit_entry.py:433-441` (バックテスト側)
- `check_signals_stop.py:144-146`, `check_signals_breakout.py` の `check_signal_on_date` (シグナル確認側)

**同名の `check_signal_on_date` が stop / breakout 双方に存在し、本体ロジックはほぼ同一**。
数式変更時は両方触る必要があります（いずれ共通化の余地あり）。

---

## 4. 戦略パラメータ (em / sm / tm)

`STRATEGY_PARAMS = { "戦略": (calc_fn, entry_atr_mult, stop_atr_mult, target_atr_mult) }`

### 逆指値B (`check_signals_stop.py:75-80`)

| 戦略 | em  | sm  | tm  | 備考 |
|------|-----|-----|-----|------|
| MACD | 0.0 | 1.5 | 3.0 | MACD(8,17,5) + 出来高スパイク + MA10 |
| A7   | 0.0 | 1.5 | 3.0 | ストキャス(14,3,3) + ATR + MA75 |
| RSI2 | 0.0 | 2.0 | 4.0 | RSI(2) + MA200 + IBS<0.35。**指値版は0.5だが stopは0.0に統一** |

### ブレイクアウト (`check_signals_breakout.py:76-80`)

| 戦略 | em  | sm  | tm  | 条件 (緩和パラメータ) |
|------|-----|-----|-----|--------|
| DON  | 0.0 | 1.5 | 3.0 | 終値 > 15日高値(前日) かつ 終値 > MA50 |
| VOL  | 0.0 | 1.5 | 3.0 | 終値 > 5日高値(前日) かつ 出来高 > 20日平均×1.5 |
| MOM  | 0.0 | 1.5 | 3.0 | ROC(10)>3% かつ MA25 > MA75 |

**全戦略が em=0.0 (終値ちょうどで逆指値)** で統一されています。
数値を変更する時は `run_limit_backtest` を再走させて WATCHLIST も更新する運用。

---

## 5. 共通定数 (backtest_limit_entry.py:47-70)

```
ENTRY_EXPIRE      = 3         # 指値/逆指値の有効日数 (シグナル発生から3営業日)
MAX_HOLD          = 15        # 最大保有日数 (超えたら "タイムカット" で終値決済)
FIXED_QTY         = 100       # 常に100株固定 (取引数量)
INITIAL_CASH      = 500_000
POSITION_SIZE     = 100_000
BACKTEST_DAYS     = 365
WORKERS           = 4         # run_signals.py の --workers デフォルト
MIN_PRICE         = 100.0
MAX_PRICE         = 100_000.0
MAX_ATR_RATIO     = 0.20      # ATR/終値 > 20% の異常ボラ銘柄は除外

# 実運用コストモデル (§14 参照)
SLIPPAGE_STOP_PCT = 0.005     # 逆指値買い +0.5% / 損切り売り -0.5% の不利約定
SLIPPAGE_LIMIT_PCT = 0.0      # 指値は注文価格ちょうどで約定
FEE_PCT_ONE_WAY   = 0.001     # 手数料 片道 0.1% → 往復 0.2%
```

状態機械は `idle → pending → in_pos → idle` (`backtest_limit_entry.py:306-415`)。
**約定と同日に決済が走るケース** (`:349-376`) は、hi/lo が両方ヒットしたら
"目標達成優先"。同日決済を無視すると検証結果と運用結果がズレるので
修正時は注意。

---

## 6. シグナル判定日のセマンティクス (過去にハマった)

- **引け後運用**: シグナル判定は当日終値で行う（寄り付き前の判定はしない）
- `target_date is None` (本日モード) のとき: `prev_idx = -1`、**最新足の `entry_sig` を見る**
  (`check_signals_stop.py:123-124`)
- `--date YYYY-MM-DD` 指定時: その日そのものを判定日とする (`prev_idx` = 指定日の行)
  (`check_signals_stop.py:125-131`)
- **過去の修正履歴 (重要)**:
  - `1182492` シグナル日をシグナル確認日と一致 (`prev_idx -2 → -1`)
  - `e7d8687` `--date` 指定時のシグナル判定を指定日の**終値**に修正
  - `300b166` `--date` 指定時はアクティブシグナルを無視
  - `b02a36a` 引け後運用に変更 (当日終値で判定)

この辺を再度触るときは上記コミットを必ず読んでから変更してください。

### 削除済み機能 (復活させない)

- `cb7d97b` **保有中追跡機能は削除** (シグナル不具合のため revert 済み)。
  「未約定シグナルを約定まで表示し続ける機能」や「保有中シグナルを損切り・利確・
  タイムアウトまで追跡する機能」は過去に入れて戻しています。再導入する場合は
  `4858cdf` / `97ae745` を参考にしつつ、シグナル判定との整合に注意。

---

## 7. おすすめスコア (calc_recommend_score)

`check_signals_stop.py:83-108` と `check_signals_breakout.py:84-109` に同一実装。
統合レポートでは両者をまぜてソートします (`run_signals.py:154-164`)。

```
score = round(
    avg_wr * 0.4                                     # 勝率      最大 40点
  + (avg_pf / 10) * 30                               # PF        最大 30点 (PF=10 キャップ、∞は10扱い)
  + stable * 20                                      # 期間安定性 最大 20点 (プラス期間/有効期間)
  + min(t_trades / 20, 1) * 10                       # 取引回数  最大 10点 (20取引で満点)
)
rank = ★★★≥80, ★★≥60, ★≥40, △<40
```

スコアは **最低1取引ある期間のみで平均** (trades == 0 の期間は除外)。
PF=∞ (損失トレード無し) は 10 として扱う、ここを変えると上位ランキングが
大きく動くので注意。

---

## 8. WATCHLIST のソース

両方ともスキャン結果をハードコードしているので、**再スキャンしない限り WATCHLIST
は更新されません**。

- `check_signals_stop.WATCHLIST` (31銘柄): `scan_entry_compare.py` パターンB
  の `--max-price 5000` スキャン上位。MACD / A7 / RSI2 それぞれで複数期間安定銘柄。
- `check_signals_breakout.WATCHLIST` (24銘柄): `scan_breakout_entry.py` 緩和
  パラメータ (DON=15日 / VOL=5日×1.5 / MOM=ROC>3%) のスキャン上位。

WATCHLIST を更新するときは:
1. `python scan_entry_compare.py --max-price 5000` などを走らせる
2. 生成HTMLから成績上位を拾う
3. それぞれの `WATCHLIST` 定数を書き換え
4. `python run_signals.py --days 365` で全期間再計算して確認

---

## 9. HTML 生成の流れ

1. `check_signals_stop.build_html(stop_items, show_days, date_label)` → フル HTML 文字列
2. `check_signals_breakout.build_html(...)` → フル HTML 文字列
3. `run_signals.build_combined_html(stop_html, brk_html)` (`run_signals.py:66-112`)
   - 各HTMLから `<style>` と `<body>` を正規表現で抜き出す (`_extract_style` / `_extract_body`)
   - ブレイクアウト固有の `.tag-don` / `.tag-vol` / `.tag-mom` CSSだけ追加
   - タブUI (`.tab-nav` / `.tab-pane` / `switchTab()`) を付けて結合

タブCSSを壊すと双方の表で色やタグが崩れるので、修正するときは両側のHTMLを開いて
目視確認するのが早い。

---

## 10. 今後の改修で想定される落とし穴・TODO

### すでに判明している共通化候補

1. **`check_signal_on_date` が stop / breakout でほぼコピペ**
   → 共通モジュール (例: `_signal_common.py`) に切り出すと両方から使える
2. **`calc_recommend_score` も stop / breakout で同一**
   → 同上
3. **逆指値価格計算式 `order_p = close + atr * em` が3箇所に分散**
   - `backtest_limit_entry.py:435`
   - `check_signals_stop.py:144`
   - `check_signals_breakout.py:145`
   → ヘルパー関数化すると em のチューニングが1箇所で済む

推定で約250行削減可能。ただし `run_signals.py` は両モジュールを
`import check_signals_stop as _stop / check_signals_breakout as _brk` で
直接参照しているので、共通化する場合はこの import 面も調整が必要
(`run_signals.py:26-27`)。

### 注意すべき副作用

- `FIXED_QTY = 100` 固定なので、コスト計算や総損益の単位は「100株 × 値差」。
  可変数量化する場合は `backtest_limit_entry.py` 全体に波及します。
- `run_signals.py` は `_stop._DEFAULT_WORKERS` を workers のデフォルトに使う
  (`run_signals.py:124`)。`check_signals_stop.py` が `WORKERS as _DEFAULT_WORKERS`
  を re-export している前提なので、後者の export を外すと壊れます。
- タブHTMLの `_extract_style` は `<style>...</style>` を1個目しか拾わない。
  将来 build_html 側で `<style>` を複数出すようになった場合は書き直す必要あり。

### やらない方が良いこと (過去の失敗)

- 保有中ポジションの追跡機能の再導入 (§6 参照)
- シグナル判定日を "翌営業日" に戻すこと (`1182492` で -2 → -1 に直した)
- `--date` 指定時に「アクティブシグナルを混在」させること (`300b166` で revert 済み)
- **現 WATCHLIST の選定方法 (同一期間でスキャン→再バックテスト)** をそのまま
  使い続けること。これは in-sample bias で勝率/PF が異常に高く出る。
  再選定するなら **Walk-forward (§13)** を使う。

---

## 11. 開発ブランチ

固定作業ブランチ:
- `claude/kabu-station-token-retrieval-hJZ29` (現行セッション)

`main` ブランチは本リポジトリには存在しないので、差分比較するときは
`git log <branch> --oneline` で上から辿ってください。

---

## 12. 参考: kabu station (別件)

`kabu_token.py` は kabuステーション REST API トークン取得用のスタンドアロン
スクリプト。現状 `run_signals.py` とは **未連携**。将来、逆指値シグナルを
そのまま kabu API に流し込む場合は、`check_signal_on_date` の返り値
(`order_price` / `stop_price` / `target_price`) をそのまま `sendorder` に渡せる
設計になっていることを押さえておいてください。接続先は デモ `18081` / 本番 `18080`。

---

## 13. Walk-forward 銘柄選定 (新パイプライン)

既存の「スキャン→同期間再バックテスト」が持つ **in-sample bias** を排除するため、
時期をずらした Walk-forward 方式で WATCHLIST を再選定する 3 スクリプトを追加しました。
**今後 WATCHLIST を差し替えるときは必ずこのパイプラインを使ってください。**

### 13.1 ファイル構成

| ファイル | 役割 |
|---|---|
| `risk_metrics.py` | MaxDD / 最大連敗 / Sharpe / 資産曲線 / リカバリーファクター |
| `scan_walkforward.py` | ユニバース (~1800銘柄) × 6戦略 × 3 fold の Walk-forward バックテスト → CSV |
| `build_watchlist.py` | CSV を読んでフィルター・ランキング → WATCHLIST 提案の Python コード |
| `verify_watchlist.py` | 提案ファイルで check_signals_stop/breakout を monkey-patch 実行 → 検証 HTML 自動表示 |

### 13.2 Walk-forward の fold 設計 (`scan_walkforward.FOLDS`)

```
Fold 1: TRAIN 730〜540日前  /  TEST 540〜360日前
Fold 2: TRAIN 540〜360日前  /  TEST 360〜180日前
Fold 3: TRAIN 360〜180日前  /  TEST 180〜  0日前
```

- TRAIN と TEST は非重複
- TRAIN (選定) は「そのパターンで銘柄を選ぶ期間」、TEST は「擬似的な未来データ」
- 3 fold 全てで TRAIN+TEST を通過した銘柄 = **時期依存しない本物の候補**

### 13.3 合格条件 (`scan_walkforward.py:65-70`)

| 区分 | trades | PF | win_rate | total_pnl |
|------|--------|-----|----------|-----------|
| TRAIN | ≥3 | ≥1.5 | ≥55% | >0 |
| TEST  | ≥2 | ≥1.2 | ≥45% | >0 |

閾値を弄るときはここを直接編集するか、引数化してください。

### 13.4 実装のキモ: df トリミングで _TODAY を触らない

`run_limit_backtest` は内部で `_TODAY - backtest_days` を cutoff に使う前提設計。
Walk-forward ではウィンドウごとに異なる start/end が必要ですが、`_TODAY` の
monkey-patch はスレッド間で競合するので、代わりに:

1. `full_df[full_df.index <= window_end]` で df 後方を事前トリミング
2. `backtest_days = (_TODAY - window_start).days` で start を指定

これで `_TODAY` を触らずに任意ウィンドウを切り出せます。ThreadPoolExecutor で
並列実行しても安全です (`scan_walkforward._run_window` 参照)。

### 13.4.5 予算フィルター (--budget / --max-price)

`FIXED_QTY=100` 株固定のため、「100株買える株価の銘柄のみ」をスキャン対象に
絞り込める `--budget` / `--max-price` オプションを両スクリプトに用意しています。

- `scan_walkforward.py --budget 600000` → `latest_price > 6000` の銘柄はスキャンせずスキップ
  (未スキャンなので計算コストも削減)
- `build_watchlist.py --budget 600000` → 既存 CSV を事後フィルター (再スキャン不要)

`latest_price` は `scan_walkforward.walkforward_one()` で
`full_df.iloc[-1]["close"]` として取得され、**予算指定が無くても** CSV に
含まれます (`walkforward_results/*.csv` の `latest_price` カラム)。
古い CSV に `latest_price` が無い場合、`build_watchlist.py --budget` は
そのフィルターをスキップします (後方互換)。

換算式: `effective_max_price = budget / 100` (FIXED_QTY=100 株想定)。
もし 1 回の発注で使う株数を将来変えるなら、ここを書き換える必要があります。

**注意点**:
- フィルターは **最新終値** (データの最終日) で判定します。過去の高値ではありません。
- 過去には手の届いた (例: シグナル発生日は 5,800円) が、直近の上昇で手の届かない
  (8,000円) 銘柄は除外されます。これは「今日から運用を始める」という前提では正しい挙動です。
- 予算ギリギリの銘柄を選ぶと、1 回のトレードでほぼ全資金を投入することになります。
  複数ポジション同時保有したい場合は `--budget (全額 / 希望同時保有数)` を指定してください
  (例: 60万円で 3 銘柄持ちたい → `--budget 200000`)。

### 13.5 実行手順

```
# Step 0 (初回のみ): ユニバース (銘柄リスト) を生成
python fetch_listed_symbols.py --market prime   # プライム ~1800銘柄 (推奨)
python fetch_listed_symbols.py --market all     # 全市場 ~4000銘柄
# → symbols_listed_prime.py / symbols_listed_all.py が生成される

# Step 1: Walk-forward スキャン (ユニバース × 6戦略 × 3 fold)
python scan_walkforward.py                      # 自動検出: prime > all > 225
python scan_walkforward.py --family stop        # 逆指値Bのみ
python scan_walkforward.py --family breakout    # ブレイクアウトのみ
python scan_walkforward.py --workers 8          # 並列数
python scan_walkforward.py --symbols symbols_listed_all.py   # 明示指定
python scan_walkforward.py --limit 50           # 先頭50銘柄だけ (デバッグ)
python scan_walkforward.py --budget 600000      # 60万円で100株買える銘柄のみ
python scan_walkforward.py --max-price 6000     # 株価6000円以下 (--budget と同義)
# → walkforward_results/walkforward_<STRATEGY>_<date>.csv  が出力される
# → CSV には latest_price カラムが含まれる (--budget 無しでも計測される)

# Step 2: CSV から WATCHLIST 提案を生成
python build_watchlist.py                       # 戦略あたり10銘柄
python build_watchlist.py --per-strategy 8      # 戦略あたり8銘柄
python build_watchlist.py --max-dd 10           # MaxDD上限10%
python build_watchlist.py --min-sharpe 0.3      # Sharpe下限0.3
python build_watchlist.py --budget 600000       # 60万円で100株買える銘柄のみ (事後フィルター)
# → watchlist_proposal_<date>.py  が出力される

# Step 3: 提案 WATCHLIST で検証バックテスト (HTML 自動オープン)
python verify_watchlist.py                      # 最新の提案ファイルを自動検出
python verify_watchlist.py --days 180           # 期間指定
python verify_watchlist.py --stop-only          # 逆指値Bのみ
python verify_watchlist.py --no-browser         # ブラウザ起動しない
# → signals_verification_<date>.html  が出力・オープン
# 既存コードは一切変更せず、ランタイムで _stop.WATCHLIST / _brk.WATCHLIST を
# monkey-patch して run_signals のバックテスト経路を再利用する。

# Step 4: 結果に納得したら手動で差し替え
# watchlist_proposal_<date>.py の STOP_WATCHLIST / BRK_WATCHLIST を
# check_signals_stop.py / check_signals_breakout.py の WATCHLIST に貼り付け

# Step 5: 旧 vs 新の比較 (必要なら)
python run_signals.py --days 365                # 差し替え後で最終確認
```

### 13.6 WATCHLIST を更新する際のチェックリスト

1. `scan_walkforward.py` を実行 → 全 6 戦略の CSV が出ているか
2. `build_watchlist.py` を実行 → `watchlist_proposal_<date>.py` を確認
3. 提案の中に **folds_passed=3 の銘柄** が最低でも数銘柄あるか?
   (ゼロの場合、閾値が厳しすぎる or 相場環境が異常)
4. `max_drawdown_pct` が全銘柄 15% 以下に収まっているか
5. `train_to_test_degradation_pct` が極端に高い (>80%) 銘柄は除外推奨
   → overfit の兆候
6. 新 WATCHLIST を `check_signals_stop.py` / `check_signals_breakout.py` に貼り付け
7. `python run_signals.py --days 365` で旧 WATCHLIST と成績を比較
8. 勝率/PF が現実的な値 (50-65% / 1.2-2.0) に落ち着いたら成功

### 13.7 WATCHLIST の「世代管理」

スキャン結果 CSV は日付付きなので、過去の選定結果を残せます。

```
walkforward_results/
  walkforward_MACD_2026-04-11.csv
  walkforward_MACD_2026-07-11.csv   ← 3ヶ月後の再選定
  ...
watchlist_proposal_2026-04-11.py
watchlist_proposal_2026-07-11.py    ← 世代比較可能
```

「前回選定銘柄が次のスキャンでも生き残るか」を見ることで、選定の再現性を
継続的に検証できます。理想的には **3ヶ月ごと** に再実行するのがおすすめ。

### 13.8 既知の限界

- **銘柄ユニバース**: `scan_walkforward.load_universe()` が以下の順で自動検出:
  1. `symbols_listed_prime.py`    (プライム ~1800銘柄) ← **推奨**
  2. `symbols_listed_all.py`      (全上場 ~4000銘柄)
  3. `symbols_listed_standard.py` (プライム+スタンダード)
  4. `symbols_all.py`             (日経225, 225銘柄) ← フォールバック

  `symbols_listed_*.py` は `fetch_listed_symbols.py` で生成。JPX の `data_j.xls`
  をダウンロードしてパースする仕組みで、7日間キャッシュ (`.jpx_listed_cache.pkl`)
  あり。JPX の Excel フォーマット変更時は `fetch_listed_symbols._parse_xls`
  の調整が必要。
- **初回実行時間**: 1800銘柄 × 6戦略 × 3fold × 2(train/test) = 約6.5万回の
  バックテスト。yfinance から各銘柄 800日分の日足をDLするため、初回は
  1〜2時間かかることがある。2回目以降は `.rsi2_cache/` のキャッシュで数分に短縮。
- **yfinance レート制限**: 1800銘柄を一気に叩くとレートリミットに引っかかる可能性。
  その場合は `--limit 500` で分割実行 → キャッシュが貯まってから全量実行を推奨。
- **FIXED_QTY=100 固定**: 銘柄間リスクが不均等。Walk-forward 検証も 100 株固定の
  前提で行うので、高ボラ銘柄が不利。将来的に volatility-parity 化する余地あり。
- **ベンチマーク比較**: §14 で `fetch_n225_return` を追加済み。build_html の
  戦略サマリーに α vs 日経 列が表示されます。
- **セクター制約なし**: 地銀 7-8 銘柄のような集中が起きうる。出力 CSV に
  セクター情報は無いので、目視で確認するか将来的に `yfinance` 等で sector
  を追加する必要がある。
- **データキャッシュ**: `backtest_limit_entry.fetch` は永続キャッシュ
  (`.rsi2_cache/`) を使う。2026-04-16 以降、キャッシュ判定は **「最新バー日付
  >= `_expected_latest_bar_date()`」** に変更。引け前にキャッシュが作られても
  引け後の実行で自動再取得されるため、**手動キャッシュ削除は不要**になった。
  `_expected_latest_bar_date()` は 平日 15:00 JST 以降は今日、それ以前は前営業日、
  週末は金曜を返す (祝日は未考慮)。

---

## 14. 実運用コストモデル & リスク指標 & フォワードテスト

バックテストと実運用の乖離を小さくするため、以下の改善を追加済みです。

### 14.1 スリッページ & 手数料モデル

`backtest_limit_entry.py:66-69` に定数を集中:

```python
SLIPPAGE_STOP_PCT  = 0.005   # 逆指値買い +0.5% / 損切り売り -0.5%
SLIPPAGE_LIMIT_PCT = 0.0
FEE_PCT_ONE_WAY    = 0.001   # 片道 0.1%, 往復 0.2%
```

適用箇所 (`run_limit_backtest`):
- **逆指値買い約定** (`:348-357`): `entry_p = limit_price * (1 + SLIPPAGE_STOP_PCT)`
- **損切り売り約定** (`:376-380`, `:428-430`): `exit_p = stop_price * (1 - SLIPPAGE_STOP_PCT)`
- **目標達成 / タイムカット**: スリッページなし (指値・成行 close で処理)
- **手数料**: 全トレードで `fee = (entry_p + exit_p) * qty * FEE_PCT_ONE_WAY` を pnl から差し引き

このモデル変更は **`run_signals.py` / `verify_watchlist.py` / `scan_walkforward.py`**
すべてに同時適用されます (モジュール定数変更だけ)。
変更後は過去の CSV / HTML の数字と **直接比較できない** 点に注意。
必要なら `scan_walkforward.py` を再実行して新しい CSV を得てください。

パラメータを変えたい場合は該当定数を編集するだけ。例: 信用取引で手数料 0.05% なら
`FEE_PCT_ONE_WAY = 0.0005` に。

### 14.2 HTML にリスク指標 & ベンチマーク α を表示

`check_signals_stop.py:181-` と `check_signals_breakout.py:186-` の `build_html`
の戦略サマリーテーブルに以下の 4 列を追加:

| 列 | 計算元 |
|---|---|
| **MaxDD%** | `risk_metrics.calc_max_drawdown` (戦略別の結合 trade_log から) |
| **連敗** | `risk_metrics.calc_max_consecutive_losses` |
| **Sharpe** | `risk_metrics.calc_sharpe` (年率換算, 1年=20取引想定) |
| **α vs 日経** | `(戦略リターン% / INITIAL_CASH) - 日経平均リターン%` |

サブタイトルに以下を表示:
- スリッページ値 (e.g. 0.50%)
- 手数料値 (e.g. 片道 0.10% / 往復 0.20%)
- 日経平均リターン (e.g. +12.3% over 365日)

日経平均は `backtest_limit_entry.fetch_n225_return(days)` で取得
(プロセス内でキャッシュ)。失敗時は 0.0 を返すので HTML には影響しない。

### 14.3 フォワードテスト (紙トレード記録)

`forward_test.py` が毎日のシグナルを CSV (`forward_test_log.csv`) に蓄積し、
実データで約定・決済を自動判定するスクリプト。

```
python forward_test.py --record            # 毎日引け後に実行
python forward_test.py --record --no-update # 既存評価をスキップ
python forward_test.py --report            # 全期間の集計レポート
python forward_test.py --report --days 30  # 過去30日分のみ
```

**動作フロー**:
1. `collect_today_signals()`: `check_signals_stop` と `check_signals_breakout` の
   全 WATCHLIST × 全戦略で今日のシグナルを `check_signal_on_date(None)` で検出
2. `evaluate_entry()`: 既存の log 行について
   - pending → fetch で最新データ取得 → 高値 ≥ 注文価格なら filled (entry_p に
     スリッページ込み)、3営業日以内に約定しなければ expired
   - filled → 目標/損切り/タイムカット (MAX_HOLD=15日) を判定、決済時の pnl を
     手数料込みで計算
3. 新規シグナルと更新されたログを CSV に保存

**CSV 行の status 遷移**:
```
pending → filled → { target, stop, timeout, holding }
pending → expired (3営業日以内に約定せず)
```

`holding` は「約定したがまだ決済していない」中間状態。次回 `--record` 時に
再評価される。

**レポートの読み方**:
- `fill率` = 約定したシグナル / 全シグナル。実運用でこれが 50% を下回るなら
  `entry_atr_mult` の調整や逆指値→成行切替を検討
- `勝率` = target / (target + stop + timeout)。バックテスト値から
  10〜30% 劣化が目安
- `損益` = 決済済みの合計円 (スリッページ・手数料込み)。プラスなら戦略が機能

運用例:
- Day 1: `python forward_test.py --record` → 今日のシグナル10件記録
- Day 2: `python forward_test.py --record` → 昨日の10件を再評価 (約定7件, 期限切3件)
  + 今日の新規シグナル記録
- Day 30: `python forward_test.py --report --days 30` → 過去30日の集計

バックテストと現実のズレを定量的に測れるので、**運用判断の根拠になる唯一の数字** です。
CLAUDE.md §10 の「やらない方が良いこと (過去の失敗)」を更新する前に、必ずフォワード
テストで裏を取ってから変更することをおすすめします。

### 14.4 注意点

- **フォワードテストは本物の取引ではない**: 実運用と違い、資金制約・保有数制約・
  税金は無視されている。「どの銘柄に入るべきか / 勝率がどう推移するか」の
  参考データとして使ってください。
- **コストモデルは近似**: 実際の手数料は証券会社プランによって大きく異なる。
  信用取引は現物よりかなり安い (0.03%程度)。逆にスリッページは個別銘柄の流動性で
  激しく変わる (出来高の少ない銘柄で -2% とかザラ)。
- **CSV のマージは注意**: `forward_test.py` は重複 (record_date + symbol + strategy)
  を排除するが、手動で CSV を編集するときは注意。最悪、ログを消して `--record`
  しなおせば OK (過去日の遡及記録はできない)。

---

## 15. トレードモード プリセット (conservative / aggressive)

目標倍率 (tm) と損切倍率 (sm) の 2 プリセットを全スクリプトで切替可能。
**デフォルトは conservative** (標準)。回転率優先の積極利確モードを使いたい場合は
`--aggressive` で opt-in。

(過去に aggressive をデフォルト化した経緯あり、2026-04-14 に conservative に戻した。
コミット履歴参照)

### 15.1 プリセット値

| 戦略 | **conservative (デフォルト)** | aggressive (opt-in) | 目標% | 損切% |
|---|---|---|---|---|
| MACD | **0.0, 1.5, 3.0** | 0.0, 1.0, 1.5 | **+9%** / 積極+4.5% | -4.5% / 積極-3% |
| A7   | **0.0, 1.5, 3.0** | 0.0, 1.0, 1.5 | **+9%** / 積極+4.5% | -4.5% / 積極-3% |
| RSI2 | **0.0, 2.0, 4.0** | 0.0, 1.2, 1.8 | **+12%** / 積極+5.4% | -6% / 積極-3.6% |
| DON  | **0.0, 1.5, 3.0** | 0.0, 1.0, 1.5 | **+9%** / 積極+4.5% | -4.5% / 積極-3% |
| VOL  | **0.0, 1.5, 3.0** | 0.0, 1.0, 1.5 | **+9%** / 積極+4.5% | -4.5% / 積極-3% |
| MOM  | **0.0, 1.5, 3.0** | 0.0, 1.0, 1.5 | **+9%** / 積極+4.5% | -4.5% / 積極-3% |

conservative は **2R 設定** (target = 2 × stop)、勝率・PF重視。
aggressive は **1.5R 設定**、回転率重視。
ATR 換算で表記しているので、実効 % は銘柄のボラにより変動します。

### 15.2 切替方法

**デフォルト (conservative) で実行**:
```
python run_signals.py                    # conservative モード (デフォルト)
python verify_watchlist.py               # 同上
python scan_walkforward.py --budget 600000
python forward_test.py --record
```

**積極利確 (aggressive) で実行 (opt-in)**:
```
python run_signals.py --aggressive
python verify_watchlist.py --aggressive
python scan_walkforward.py --aggressive --budget 600000
python forward_test.py --record --aggressive
```

実際の切替: 各スクリプトの **最初の import より前** に `sys.argv` を
チェックして `os.environ["TRADING_MODE"]` を設定する。
その後 `check_signals_stop` / `check_signals_breakout` / `scan_walkforward`
が import されると、モジュールトップで env var を読んで
`STRATEGY_PARAMS` / `STRATEGY_DEFS` をプリセットに差し替える。

**環境変数 (シェル全体で固定)**:
```
export TRADING_MODE=aggressive   # 常に aggressive で固定
python run_signals.py            # --aggressive 不要
```

### 15.3 出力ファイル名の分離

**デフォルト (conservative) は suffix なし**、**aggressive は `_aggressive` suffix**。

| スクリプト | conservative (デフォルト) 出力 | aggressive 出力 |
|---|---|---|
| run_signals.py | `signals_combined_YYYY-MM-DD.html` | `signals_combined_aggressive_YYYY-MM-DD.html` |
| verify_watchlist.py | `signals_verification_YYYY-MM-DD.html` | `signals_verification_aggressive_YYYY-MM-DD.html` |
| scan_walkforward.py | `walkforward_STRATEGY_YYYY-MM-DD.csv` | `walkforward_STRATEGY_aggressive_YYYY-MM-DD.csv` |
| forward_test.py | `forward_test_log.csv` | `forward_test_log_aggressive.csv` |

### 15.4 運用上の注意

- **WATCHLIST は互換**: aggressive モードに切替えても、`check_signals_stop.WATCHLIST` /
  `check_signals_breakout.WATCHLIST` の銘柄リストはそのまま使える。ただし
  **各戦略に最適な銘柄構成はモードごとに違う可能性**がある (aggressive は小さな
  利幅を取るため、よりトレンドが弱くても勝てる銘柄が向く)。
  理想的には `scan_walkforward.py --aggressive` で aggressive 用 WATCHLIST を
  別途作り、提案ファイル → `check_signals_*.py` に貼り付けると良い。
- **バックテスト数字の比較は同モード内で**: conservative と aggressive の
  数字を直接比較すると誤解を生む。例えば aggressive は「勝率高いが総損益は低い」
  ケースが多い。どちらが優れているかは Sharpe 比率や期待値で判断するのが妥当。
- **フォワードテストはモードごとに別ログ**: `forward_test_log.csv` (conservative)
  と `forward_test_log_aggressive.csv` が独立。両方記録したければ 2 回実行:
  ```
  python forward_test.py --record                # conservative
  python forward_test.py --record --aggressive   # aggressive
  ```
- **ENTRY_EXPIRE / MAX_HOLD は共通**: 現状プリセットで変えていない
  (3 営業日有効 / 15 日保有上限)。より回転率を上げたいなら `backtest_limit_entry.py`
  の `MAX_HOLD = 15 → 7`, `ENTRY_EXPIRE = 3 → 2` を検討。こちらはモジュール
  定数なので手動書き換え。

### 15.5 A/B テストの流れ (推奨)

```
# 同じ日に 2 モード実行して数字を比較
python run_signals.py                  # conservative (デフォルト)
python run_signals.py --aggressive     # aggressive

# 並べてブラウザで開き、以下を比較:
#  - 勝率 (aggressive の方が高いはず)
#  - PF (conservative の方が高いはず)
#  - 総損益 (年間で勝つ方を選ぶ)
#  - 平均保有日数 (aggressive は短い)
#  - Sharpe 比率 (リスク調整後)

# フォワードテストも 2 モード並行で記録
python forward_test.py --record                # conservative ログ (デフォルト)
python forward_test.py --record --aggressive   # aggressive ログ

# 1ヶ月後、それぞれレポート
python forward_test.py --report                # conservative 実績
python forward_test.py --report --aggressive   # aggressive 実績
```

実運用のどちらが優秀かは **バックテストではなく フォワードテストの実績** で判断してください。

---

## 16. デイトレ: ギャップ反転の日足検証 (`gap_reversal_daily.py`)

**同日決済（寄りで建てて引けで閉じる）** の仕組みを検討するための検証スクリプト。
逆指値シグナル (§1〜§15) とは独立で、`run_signals.py` 系には一切影響しません。

### 16.1 検証している仮説

> 前日に大きく動いた銘柄が、翌朝さらに同じ方向へギャップして寄ると、その日のうちに戻る。

上げ側にギャップ → 空売り、下げ側 → 買い。始値で建てて引けで決済。

### 16.2 なぜ日足だけで足りるか

このルールの判定に必要なのは `前日終値 → 始値 → 終値` の 3 点だけで、
**日中の価格の到達順序を必要としません**。したがって分足 (直近2年) ではなく
日足 (2007年〜) の全期間で検証できます。分足が要るのは「決済を引けより前に
早めるか」「損切りを置くか」を決めるときだけ。ここを混ぜると、19年ぶんの
証拠を捨てて2年で判断することになります。

### 16.3 叩き台からの3つの変更

1. **残差ギャップ化** — 単純なギャップは市場要因を含む。反転するのは
   個別銘柄が市場から乖離した分だけ。ローリング120日β (t-1 までの情報のみ) で
   `resid_gap = gap - beta * 指数ギャップ` を計算する。
2. **ATR 正規化** — `+1.75%` / `+1.0%` の固定閾値は、日経が2%動いた日に全銘柄が
   引っかかり、凪の日は何も出ない。ATR20 で割ると候補数が安定する。
   固定閾値版は `--raw` で再現可能。
3. **決算日の分離** — 大きく動いた翌日のギャップの相当数は決算。決算ギャップは
   反転せず同方向にドリフトする (PEAD)。`--earnings` で分離して集計する。
   決算グループが逆符号にならないなら、「過剰反応の巻き戻し」という説明自体が誤り。

### 16.4 レポートの読み方 (7セクション)

| # | 内容 | 落第条件 |
|---|---|---|
| 1 | 生リターン と α (同日ユニバース平均を控除) | α が消える = 市場の日中ドリフトを拾っていただけ |
| 2 | トレード単位 t vs 日次集約 t | **同じ日のシグナルは独立ではない**。日次集約の方だけを見る |
| 3 | 年別 α | 特定の年だけで稼いでいる |
| 4 | ギャップ幅の5分位別 α | 単調に増えない = ノイズ |
| 5 | プラセボ (前日の動きなし / 符号反転 / 決算あり) | 予想通りに壊れない |
| 6 | コスト感応度 | エッジ/コスト比 < 3 |
| 7 | 資金制約下 (1日13銘柄上限) と資金換算 | 執行可能な上位だけにすると消える |
| 8 | 裾の依存度 (中央値 / 上位5日の寄与 / 上下1%除去後) | 上位10%の日で総損益の9割 = 少数の極端な日頼み |
| 3b | 月別の寄与 (§3 の末尾) | 最大の1ヶ月がグロス総和の40%超 = 単月依存 |
| 9 | 方向別 (買い / 空売り、トレード単位と日次を並記) | 空売り側にしかエッジが無い / 単位で符号が逆転する |
| 10 | 引けで決済できない恐れ (値幅制限) ＋ headroom 5分位 | 買い側はストップ安で売れない / 空売り側はストップ高で買い戻せない |
| 11 | 前夜の売買代金 上位N位のカバー率 (N=13/25/50/100/200) | 監視枠に入らない発火が多い = 運用が成立しない |
| 12 | 生の騰落率ランキング 上位N位のカバー率 (N=10/20/30/50) | kabu の `/ranking` で拾えるか。N=30 で95%未満なら不足 |

**§11 は件数だけでなく「利益の何%を拾えるか」も出します。** 売買代金の大きい
銘柄ほどギャップが小さければ、件数を半分拾っても利益は3割、が起こります。
枠は執行方式で変わります。

| 執行方式 | 前夜に絞る枠 | 気配の判定誤差 |
|---|---|---|
| 板を読んで判定 → 発注 | 板の購読 **50銘柄** | 乗る |
| 前夜にトリガー価格の寄指を置く | 予算 = 建玉30万/資金400万で **13件** | **ゼロ** |

`--side long` / `--side short` で片側だけに絞って全検定を通せます。
制度的制約も決済リスクも方向で全く違うので、実運用の設計はまず片側で考えること。

**採用の目安**: 日次 t > 3 かつ 年別で大半がプラス かつ 単調性あり かつ
エッジ/コスト比 > 3 かつ 裾を落としても t が残る かつ 単月依存でない。

⛔ **裾1%除去では単月依存を検出できません。** 1トレード単位の外れ値を落として
いるだけなので、暴落月のように「1ヶ月まるごと」効いている依存は素通りします。
§3 末尾の月別寄与を必ず見てください (分足2年の検証では 2025-04 の1ヶ月だけで
グロスの 65% を占めていました)。ここを通って初めて、
分足での執行検証に進む価値がある。

### 16.4.4 ⛔ `--prev-thr 0.0` は「条件なし」ではない

シグナルの条件はこうです。

```
up = (prev_z >= prev_thr) & (gap_z >= gap_thr)
dn = (prev_z <= -prev_thr) & (gap_z <= -gap_thr)
```

`prev_thr = 0.0` にすると `prev_z >= 0` が残るので、**前日の動きがギャップと
同符号であること**を要求します。「条件なし」ではありません。実測でギャップ事象の
約半分が落ちます (空売り側 5,112 件 → 条件を外すと 9,896 件)。

本当に条件を課さない場合は `--prev-thr none` を使ってください。
`--grid` は「なし」の行を先頭に持ち、`0.00` の行との差が
**同符号を要求することの効果**そのものになります。

⛔ 2026-08 までの結果 (19年 1,615件 / t=5.38 など) はすべて `prev_thr = 0.0`、
つまり**同符号を要求した部分集合**の数字です。「前日の条件を落とした」と
説明していましたが、正確には「大きさの条件だけ落とした」でした。

### 16.4.5 閾値グリッド (`--grid`) — 台地か尖りか

閾値を1組だけ試して良い数字が出ても、それは「データを何度も見た結果その組が
当たっただけ」かもしれない (多重比較)。`--grid` は前日の動き × ギャップの
7×7 を総当たりし、各区画の 日次t / グロスα / 件数 を出す。

- **t が高い区画が面で広がっている** → エッジは本物
- **1区画だけ高く周囲が低い** → その閾値は偶然の可能性が高い
- **件数が3桁を切る区画** → t の値によらず信頼できない
- **前日 0.00 の行が下の行と同じ** → 前日の動きという条件は効いていない
- **端の列で単調増加が続く** → 探索範囲が狭い。`--grid-gap` で伸ばす

前日 0.00 (条件なし) の行を含めるのが要点。パネルは `|gap_z| >= 0.3` の行を
必ず保持するので、ギャップ閾値 0.3 以上の区画は前日閾値によらず母集団が完全。
出力は t / グロスα / 件数 の3表で、最後に最良区画とその再現コマンドを出す。

```
python gap_reversal_daily.py --grid
python gap_reversal_daily.py --grid --grid-gap "1,2,3,4,5" --grid-prev "0,1,2"
```

```
python gap_reversal_daily.py --grid
```

グリッド時はパネルを最も緩い閾値 (前日 0.9 ATR) で構築するので、
通常実行よりメモリを使う。

### 16.3.5 ⛔ 始値ノイズによる見せかけのエッジ

`gap = 始値 / 前日終値` と `o2c = 終値 / 始値` は **同じ始値を分子と分母に
共有している** ため、始値の測定ノイズ (板寄せの偏り・気配のバウンス) だけで
機械的に負の相関を持ちます。**ランダムウォークでも必ず出ます。**

実測: 合成ランダムウォーク (エッジ0) で α = +42bp / t = 27。控除すると +2.6bp / t = 1.7。

レポート §1 の 3 行目「α (始値ノイズも控除)」が、`o2c ~ gap` の回帰の傾き分を
差し引いた値です。**この行が消えるなら、それは過剰反応の巻き戻しではなく
微細構造ノイズ**なので、そこで打ち切ってください。

**傾きは |ギャップ| の十分位ごとに推定し、控除には「最小帯域の傾き」を使います
(2026-08 に2度修正)。**

始値に乗算ノイズ ε があると `gap = 真のギャップ + ε` / `o2c = 真の日中変化 - ε`
となり、**|ギャップ| が小さい帯域ではノイズが分散を支配するので傾きは -1 に
近づきます**。逆に大きいギャップの帯域ではノイズの寄与は無視できます。
したがってノイズ由来の成分は最小帯域から推定するのが正しい。

⛔ **シグナル自身の帯域の傾きを引いてはいけません。** それは「反転はギャップに
比例する」という効果そのものの控除になり、比例する本物の効果まで消します。
一度この誤りを犯し、α 56.8bp を 12.8bp まで削って「棄却」と誤読しました。

読み方 (§1 の下に帯域表が出ます):

- **最小の帯域が強く負 (-0.1 以下など)** → 始値ノイズの兆候。3行目が消えたら打ち切り
- **最小の帯域が 0 か正** → 始値ノイズは実質ゼロ。この経路では棄却できない

実測 (東証プライム 1,362銘柄 / 2007-2026):

```
0.00% 〜 0.03%   +1.1120     ← 負でない = ノイズなし
0.03% 〜 0.17%   +0.1310
0.31% 〜 0.46%   -0.0141
0.82% 〜 1.07%   -0.0418
2.04% 以上       -0.0367     ← 効果そのもの
```

日本株の寄り付きは板寄せで単一の約定価格が付くため、始値に気配のバウンスが
乗らないのは自然な結果です。**日本株ではこの経路での棄却は期待できません。**

⛔ この診断はデータ破損があると機能しません。ギャップ 122万% のような行が
説明変数に入ると回帰の傾きが 0 に潰れます。§16.5.5 の整合性チェックを必ず
先に通してください。

### 16.5 使い方

```
python gap_reversal_daily.py --coverage       # データの被覆状況 + 年別の銘柄日数
python gap_reversal_daily.py --grid           # 閾値グリッド (台地か尖りか)
python gap_reversal_daily.py                  # .rsi2_cache/ を自動検出して実行
python gap_reversal_daily.py --self-test      # 合成データで配管を確認 (要ネットワーク無し)
python gap_reversal_daily.py --limit 200      # 200銘柄で試す
python gap_reversal_daily.py                  # 全ユニバース
python gap_reversal_daily.py --raw            # 1.75%/1.0% の固定閾値版
python gap_reversal_daily.py --earnings       # 決算日を取得して分離
python gap_reversal_daily.py --cost-bps 40 --max-names 13
python gap_reversal_daily.py --csv panel.csv  # 候補パネルを書き出す

# ローカル日足を使う (yfinance を叩かない)
python gap_reversal_daily.py --data-dir ./daily        # <SYM>.csv/.parquet/.pkl を並べたディレクトリ
python gap_reversal_daily.py --data-file all.csv       # symbol,date,ohlcv の長形式1ファイル
python gap_reversal_daily.py --data-dir ./daily --no-index   # 指数ファイルが無い場合
```

**引数なしで実行すると `.rsi2_cache/` を自動で使います** (`--cache-dir` で変更、
`--no-cache` で無効化)。`backtest_limit_entry.fetch` が作った `<7203_T>.pkl` を
そのまま読み、`7203_T` → `7203.T` に読み替えます。250本未満の銘柄は除外。
指数 `^N225.pkl` がキャッシュに無ければ yfinance から取りに行き、それも不可なら
`--no-index` を促して終了します。

`--coverage` は履歴の開始日の分布 (最古 / 中央 / 90%点) と最終日を出して終了します。
`.rsi2_cache/` は `fetch` が開始日を見ない設計のため **2007年まで遡っていない
ことが多い**ので、長期の結論を出す前に必ず確認してください。中央値で5年未満の
場合は警告が出ます。

`--data-dir` / `--data-file` を指定すると yfinance を一切使いません
(ネットワーク不可の環境用)。列名は大文字小文字を問わず
`open/high/low/close/volume` (+ 日付は `date`/`datetime`/index のどれでも可)。
ファイル名 `7203_T.csv` / `7203.csv` / `72030.csv` はいずれも `7203.T` として
も引けます。市場成分の控除には指数 (既定 `^N225`) が必要で、同じディレクトリに
`^N225.csv` を置くか `--index-symbol` で名前を指定します。無い場合は
`--no-index` (β=1・指数リターン0 扱い = 残差ギャップが生ギャップになる)。

`--self-test` は既知の 25bp のエッジを埋め込んだ合成データを流し、レポートが
それを検出できるかを確認します (ネットワーク不要)。ロジックを触ったら必ず実行。

### 16.5.5 ⛔ データ破損の除外 (必須)

`.rsi2_cache/` には **始値が壊れている行**が混ざっています。実測で
「ギャップ 1,227,196%」「Q5 の上限 394,860,203 ATR」が出ました。
`backtest_limit_entry.fetch` は終値の前日比 50% 超しか検査しないため、
**始値は素通り**します。

`gap_reversal_daily` は2段構えで除外します。

1. **OHLC 整合性** — 始値・終値は必ず 安値〜高値 の内側にある。外れていたら破損
2. **値幅制限** — 東証には値幅制限があるので、1,000〜6,000円の株が1日に
   30% を超えて動くことはあり得ない。`|gap|` `|o2c|` `|前日比|` が
   `MAX_GAP` (既定 0.30) を超える行を除外 (`--max-gap` で変更可)

除外件数はパネル構築後に必ず表示されます。内訳は `--audit` で一覧できます。

```
python gap_reversal_daily.py --audit
```

- **始値だけ桁違い** → yfinance の誤植
- **OHLC 全体がきれいな整数倍** → 株式分割の未調整

いずれも該当銘柄のキャッシュを消して取り直してください。

**この汚染は `--prev-thr 0.0` にした瞬間に効きます。** 破損行は終値が正常なので
前日リターンが小さく、前日閾値を課している間は偶然弾かれています。閾値を緩めた
途端に流れ込むので、「緩めたら成績が上がった」ときは真っ先に破損を疑うこと。

### 16.6 データ

- 日足キャッシュは **`.gapmr_cache/`**（`.rsi2_cache/` とは別）。
  `backtest_limit_entry.fetch` はキャッシュの**開始日を見ない**ため長期検証に
  使えません。`gap_reversal_daily.fetch_daily` は開始日も検証します。
- 初回は 2007年以降の日足を全銘柄ぶん取得するので時間がかかります。
- 決算日は `yfinance.get_earnings_dates` (直近数年のみ)。取れない銘柄日は
  除外せず「決算情報なし」グループとして別集計します。

### 16.6.3 ⛔ 探索の記録 (結果を見て決めたことの一覧)

閾値を動かして通したのと、母集団を分けて通したのは、探索としては同型です。
**結果を見てから決めたことは、全部ここに数える。**

| # | 決めたこと | 何を見て決めたか | 効果 |
|---|---|---|---|
| 1 | 前日の「大きさ」の条件を外す (同符号は残す) | グリッド | 件数を増やした |
| 2 | ギャップ閾値 0.5 → 3.0 | グリッド | エッジ比 0.71 → 2.99 |
| 3 | **買い側単独に絞る** | §9 の方向別 | エッジ比 2.99 → **4.70** |

1 と 2 はグリッドが台地を示したので、単一点を拾った探索ではありません。
**3 は違います。** 基準未達 (2.99) だったものが、母集団を半分にしたら超えた
(4.70) という形で、閾値を動かして通したのと構造的に同じです。

⛔ **買い側を選ぶ理由は2種類あり、記録上分けること。**

| 種類 | 内容 | 正当性 |
|---|---|---|
| 成績で選んだ | エッジ比 2.99 → 4.70 | **探索の1手**。多重比較の対象 |
| 制度で選んだ | 空売り規制・貸株の可否・増担保・逆日歩がない | **事前判断**。成績を見る前から分かる |

後者だけなら正当ですが、両方が混ざると「どちらで決めたか」が後から分からなく
なります。現時点では**両方が混ざっている**のが正直な状態です。

### 16.6.35 ⛔ 買い側はサバイバーシップに最も弱い

買い側の機構は「暴落後の反発を拾う」。未検証の最大項目は
「下げ続けて上場廃止になった銘柄が抜けている」。
**バイアスが最も効く場所に、最も強い結果が出ている形**です。

`--side long` の採用は、これを測る前には決められません。既存データで測れます。

```
python gap_reversal_daily.py --prev-thr 0.0 --gap-thr 3.0 --side long
```

§3 の末尾に期間分割 (既定 2007-2012 / 2013-2019 / 2020-2026) が出ます。

⛔ **単調性だけを見てはいけません。** 3点が偶然単調に並ぶ確率は 1/6 = 17% です。
最古と最新の差を、標準誤差付きで検定します (`最古 − 最新 = ±X bp (SE, t)`)。

- **t > 2 で最古が強い** → サバイバーシップの疑い
- **|t| < 2** → **検出も棄却もできない**。「バイアスは小さい」ではなく
  「この標本では分からない」が正しい結論

境界は `--split-dates` で変更できます。

**実測 (買い側 548件 / 前日 0.0 / ギャップ 3.0)**

```
2007-06〜2013-01   119件  グロス 174.0   日次 93.7   t=2.80
2013-01〜2020-01   106件  グロス 136.9   日次 105.8  t=2.82
2020-01〜2026-08   323件  グロス 130.1   日次 92.1   t=4.75
```

グロスは単調減少ですが、**日次bpは 93.7 → 105.8 → 92.1 で単調ではありません**。
per-trade の標準偏差を 600〜1,500bp と見積もると 最古−最新 の t は 0.3〜0.7 で、
**差は完全に誤差の範囲**です。この標本ではサバイバーシップを検出も棄却もできません。

測るには上場廃止銘柄を含むユニバースが要ります (`.rsi2_cache/` は現行の
プライム構成銘柄のみ)。**買い側の採用は、それを揃えるまで保留が妥当です。**

### 16.6.38 ⛔ 買い側の正しいヘッドラインは 74.14bp / t=3.98

§10 の持ち越し評価 (2026-08 追加) の結果、**引けで決済できなかった建玉を
翌営業日の始値まで持った場合**の数字が出ました。

```
引けで決済できた前提    -81.9bp
翌日始値まで持ち越し   -355.7bp   差 -273.7bp (中央 -51.0)
悪化した件数 24 / 47 (51%)
────────────────────────────────────────
持ち越しを織り込んだ全体  74.14bp  t=3.98   (95.74bp / t=6.15 から低下)
```

**持ち越しは選択肢ではなく、月0.21回起きること**なので、こちらを採用します。
α が 22.5% 削られ、t も 6.15 → 3.98 に落ちます。基準 (t>3) は通りますが
余裕が減りました。中央値 -51.0bp に対し平均 -273.7bp なので、**少数の
大きく飛んだ日**が効いています。

以下の 95.62bp / t=6.16 は持ち越しを織り込む前の数字です。

### 16.6.4 実測: 買い側が全軸で優位 (前日 0.0 / ギャップ 3.0 / 19年)

|  | 買い側 | 空売り側 |
|---|---|---|
| 日次 α (コスト30bp後) | **95.62bp / t=6.16** | 40.46bp / t=3.07 |
| グロス | **140.9bp/件** | 63.4bp/件 |
| エッジ/コスト比 | **4.70** | 2.11 |
| ストップで決済できない恐れ | 8.58% / 月0.20件 | 10.97% / 月0.51件 |
| 制度的制約 | なし | 空売り規制・貸株・増担保 |

全体の 2.99 は弱い空売り側に引きずられた数字でした。**実運用を考えるなら
買い側単独** (`--side long`)。

⛔ §10 の「該当を除いた成績」(56.84 → 81.66bp) は良い知らせではありません。
除外された164件は既に大負けしている日で、**どの日がそうなるかは事前に
分かりません**。除外は運用できない操作です。意味は逆で、月0.5件の頻度で
「引けで決済できないかもしれない」場面が来るということ。決済できなければ
翌日に持ち越して損失が拡大しうる (未モデル化)。

参考: 別戦略 (N ルール / ギャップ1%前後 / 11.6年) のストップ高は
**58件 / 月0.4件**、買い側は 11.6年で0件。±11.9% の水準では桁が違います。

⛔ **訂正 (2026-08-29)**: 以前記録していた「91件 / 月0.7件」は**先読み込み**の
数字でした。監視枠 (watch50) の候補プールを当日始値で絞っていたため、
前夜には選べない銘柄が混ざっていました。差の 121件 のうち **35件 (29%)** が
持ち越し候補で、全体率 0.44% の **66倍**に集中していました。

機構: 前日終値が 1,000円未満の低位株が +100bp 以上ギャップアップして
1,000円台で寄る。低位株は値幅制限が株価比で広いのでストップ高になりやすい。
**正しい仕様ではこれらは watch50 に入らないので建てられません。**
除外は正しく、独立実装の ES は正確です (資本 470万〜500万は訂正不要)。

⚠ 「同時多発は未観測」は **3件以上について正しい**。2件同時は4回あるので
「完全に孤立」は言い過ぎでした。

### 16.6.5 方向で制度的制約が全く違う

空売り側 (ギャップアップを売る) には **空売り規制・貸株の可否・増担保・
値幅制限** が効き、板も薄い。買い側 (ギャップダウンを買う) にはこれが無い。
レポート §9 で必ず分けて見ること。

- 買い側だけで成立する → 制度的制約を回避できる。実装が一気に楽になる
- 空売り側にしか無い → 実弾の前に証券会社への照会が必須
  (`check_shortable.py` / `not_shortable.py` / `check_price_limit._TSE_LIMIT_TABLE`)

大きなギャップ (2.5 ATR 以上 = 株価で 5〜6%) はほぼ決算・材料日なので、
往復 30bp というコスト仮定はこの水準では楽観的すぎる可能性が高い。
§6 のコスト感応度を 80bp まで見て判断すること。

### 16.8 ⛔ 始値では建てられない (`gap_reversal_intraday.py`)

`gap_reversal_daily.py` は 始値→終値 で成績を測りますが、**これは執行できません**。
残差ギャップは当日の始値と指数の始値を見て初めて計算できるので、09:00 の
板寄せで始値が付いた後に判定するなら、実際の約定はその次の売買価格です。
事前に条件付きの寄成注文を置ける注文仕様が無い限り、日足の 始値→引け 成績を
そのまま執行可能な成績と見なすことはできません。

`gap_reversal_intraday.py` が分足で価格の階段を測ります。

| 価格 | 意味 |
|---|---|
| 日足の始値 | 条件判定に使う価格。**執行不能** |
| 分足の板寄せ価格 (最初のバーの始値) | 同上。**執行不能** |
| **最初のバーの終値** | 板寄せ後、**実際に入れる最速の価格** |
| +1分 / +5分 / +10分 / +30分 | 遅延による劣化 |

さらに、入る時点までの出来高 × 割合 を各銘柄の上限として資金制約をかけ、
買い側 / 空売り側 を完全に分離して集計します。

```
python gap_reversal_intraday.py --self-test
python gap_reversal_intraday.py                      # 1分足を自動検出
python gap_reversal_intraday.py --interval 5m
python gap_reversal_intraday.py --max-vol-share 0.05
```

**ホールドアウト**: 分足があるのは 2024-07 以降。閾値・ユニバース・残差化の
設計は日足側で探索して決めた経緯があり、t は楽観側に寄っています。
分足期間は **ルールを一切触らない最終ホールドアウト**として扱い、
`FROZEN_PREV_THR` / `FROZEN_GAP_THR` を凍結したまま執行モデルだけを検証します。
**ここで閾値を動かした瞬間、ホールドアウトではなくなります。**

分足の落とし穴 (N_CORE.md より):
- 5分足は**株式分割が未調整**。同じ日の分足終値の中央値と日足終値が 30% 以上
  ずれたら、その銘柄日を捨てる (実装済み)
- **先頭バーが 09:00 とは限らない**。09:05 始まりの銘柄日がある
- 11:30〜12:30 は昼休みでバーが無い。等間隔を仮定しない
- 銘柄コードが2種類。分足は J-Quants の5桁 (`72030`)、日足は `7203.T`

### 16.9 執行可能性: 寄成で板寄せに参加できる (2026-08 実測)

**「始値では建てられないので原理的に執行不能」は誤りでした。** 実データ
(ホールドアウト 363件 / 181日) で生の分足の先頭時刻から測った結果:

```
09:00 ちょうどに寄った          5 / 363 件 (1.4%)
寄りの遅れ                     中央 14分 / 75%点 19分 / 90%点 26分
corr(|ギャップ|, 遅れ)          +0.590
ギャップ分位別の中央の遅れ        Q1 7分 → Q2 11.5 → Q3 13 → Q4 16 → Q5 24分
```

**98.6% が遅れて寄り、ギャップが大きいほど遅い。** 大きく動く銘柄ほど特別気配で
寄り付きが遅れるという制度と整合します。したがって:

1. 対象銘柄が寄る時点で、指数の始値は確定している
2. 残差ギャップを計算する時間が中央値14分ある
3. **寄成注文を出して板寄せに参加すれば、始値そのもので約定できる**

執行価格ごとの成績 (ホールドアウト、往復30bp):

```
板寄せ価格 (寄成で参加可)     +47.32bp   t= 2.03
────────────────────────────────────────────
最初のバーの終値 (板寄せ後)   -17.77bp   t=-0.83
+5分                       -58.25bp   t=-3.55
```

**板寄せに乗れるかどうかが全て**で、乗り損ねると 65bp 失われます。+5分・+10分が
強く負なのは、板寄せの行き過ぎが即座に是正された後はギャップ方向へドリフトが
続くためで、遅れて入るのは有害です。

⛔ 他セッションが出した `delay_minutes` は信用できませんでした
(corr が -0.087 と負、ギャップ5分位で delay=0 が中間3分位だけ厳密に0件、
「5分足」ファイルの entry_time の 69.6% が5分の倍数でない)。ただし**遅延という
現象自体は実在**します。生バーで確認済み (7740/2026-06-17 は 09:25 に出来高
535,900 で寄り、6480.T/2026-05-12 は 09:49 に 1,253,500 で寄った)。
**集計の欠陥と現象の不在を混同しないこと。**

### 16.9.0 ⛔ 執行価格が始値に化ける事故

別途行われた検証で「最初の分足の価格は公式始値からほぼ劣化しない
(entry_drag = 0.03bp)」という結果が出ましたが、trades.csv を検査したところ:

- `entry_price` が `official_open` と **完全一致: 1,117 / 1,121 行 (99.6%)**
- 一致しなかった 4 行の乖離は 中央値 17bp / 最大 68bp

一致自体は「遅れて寄るので始値=その遅れた約定価格」でも説明がつくため、
これだけでは事故と断定できません (私は一度断定して誤りました)。
判別には生の分足を見る必要があります。`--dump-bars N` を使ってください。

`gap_reversal_intraday.py` は執行価格が板寄せ価格と一致する割合が 5% を
超えると警告し、【0】で先頭バーの出来高0率・寄りの遅れの分布・ギャップ分位との
クロス集計を出します。**自分の集計が同じ罠を踏んでいないかの検査**です。

### 16.9.1 残っている未検証項目

板寄せに参加する経路は開きましたが、以下は履歴では検証できません。

- **気配 → 板寄せ価格 の予測誤差。** 寄る直前の気配は板寄せの均衡価格に
  収束しますが、完全ではありません。外した分だけエッジが削れます
- **自分の注文が板寄せ価格を動かす影響。** 400万円は 1,930円の銘柄で約2,000株、
  板寄せ出来高 125万株に対し 0.16% なので小さいはずですが、未測定です
- **往復30bp というコスト仮定。** エッジ/コスト比は 2.99 でぎりぎり。
  50bp で t=3.49、80bp で t=0.65 と消えます

検証は前進テストのみ: 毎朝、対象銘柄の寄り前気配を記録 → 残差ギャップを計算 →
寄成を出したと仮定 → 実際の始値と突き合わせる、を最低3ヶ月。

### 16.7 既知の限界

- **サバイバーシップバイアス**: `symbols_listed_prime.py` は今日の上場銘柄。
  2007年から回すと途中で消えた銘柄が抜ける。買い側 (暴落後の反発) で特に効く。
- **執行は検証できない**: 板・気配の履歴が無いので「09:00 に本当にその始値で
  建てられるか」は履歴では分からない。実弾の前に、毎朝 08:59 の気配で候補を
  確定 → 想定約定価格を記録 → 実際の始値と比較、を最低3ヶ月。
- **コストは仮定**: デフォルト往復 30bp (呼値 + 板寄せ + 引け成行)。
  3,000円以上は呼値5円なので実効コストが倍になる点に注意。
- ストップ高で引けに買い戻せない事故は §10 で件数を測ります (2026-08 追加)。
  終値と前日終値から東証の制限値幅 (`tse_price_limit`) でストップ値を判定し、
  買い側は下限一致、空売り側は上限一致の件数を出します。
  **終値がストップ値でも板寄せは成立しているので比例配分で決済できた可能性が
  あり、出る数字は「決済できなかった恐れの上限」です。** 連続ストップ時の
  値幅拡大は未考慮なので、その分は過小評価に振れます。

  制限値幅テーブルは実データで検算済み: 前日終値 1,530円 → 値幅 400円 →
  ストップ高 1,930円 (+26.14%)。観測された 2767.T / 6480.T の値と一致。

### 16.10 判定に使う集合は「順位フィルタ後・持ち越し後」(§13)

§3 の年別と §8 の裾は **全シグナル・持ち越し無視** で測っています。実際に運用
できるのは、kabu の `/ranking` 上位50に入り (§12。⛔ 30件ではなく **50件**。
2026-08-30 実測)、かつ引けで決済できなかった日は
翌朝まで持ち越した集合 (§10) だけです。**判定に使う数字は §13 に揃えてください。**

- 年別は 3点の単調性ではなく **前半/後半の差を SE 付きで検定** します。
  §16.6.35 と同じ理由で、少数点の並びは偶然で単調になります。
  `|t| < 2` は「直近が弱くない」ではなく **「この標本では分からない」** です。
- 裾も順位フィルタ後・持ち越し後で測り直します。上下1%除去は
  **1ヶ月まるごとの依存を検出できない** ので、年別と併せて見ること。

現時点の主判定は **N=30 / 持ち越し後 66.03bp / t=3.14**。ただし
持ち越し前の t が 5.47→6.15 と滑らかなのに対し、持ち越し後は
3.69/2.99/3.14/3.81 と跳ねるので、**3.14 は点推定ではなく 2.99〜3.81 の帯**です。
さらに D+2 の張り付きが 4/47 件あるため -355.7bp は下限で、**t=3.14 は上限**です。

### 16.10.5 ⛔ 同じ量を3通りに測って「1つだけ有意」と読まない

減衰の検定は3つありますが、**同じデータの同じ量を3通りに測ったもの**です。

```
期間分割        +95.0bp  (SE 38.9, t=2.44)
前半後半 等重み   +46.1bp  (SE 36.0, t=1.28)
      取引数重み   +64.4bp  (SE 32.9, t=1.96)
```

「1つだけ有意だから断定できない」も「1つ有意だから減衰した」も不適切です。
正しい読み方は **点推定は3つとも同じ向き、大きさ +46〜+95bp、t は 1.28〜2.44。
方向は一致し、大きさが推定量の選び方で2倍動く。**

そして実務に効くのは t ではなく水準です:
**〜2022 が 77.27bp、2023〜 が 24.31bp = −68%。** これが計画値の根拠。

### 16.11 ⛔ やり直さないこと

1. **始値ノイズの推定** (§16.3.5)。帯域を `rank<=30` などで絞ると、ノイズが
   支配する最小帯域が壊れて傾きが非単調になり、**ノイズ推定が存在しなくなります**。
   ノイズは **価格データの性質であってトレード選択の性質ではない**。
   再検査するなら **銘柄** (呼値の粗さ・流動性) で条件付けること。
   **ギャップ幅で絞ってはいけません** (効果そのものを引くことになる)。
2. **指数ギャップが大きい日の除外ルール**。該当は 2 件しかありません。
   前進記録の **観測列** としてだけ登録し、ルールは作らないこと。

### 16.12 始値ノイズは (a)(b) で閉じた — 「未判定」で置かない

§16.3.5 の帯域推定は「この母集団では識別できない」ことしか言えません
(帯域を絞ると壊れるので、選択後の条件付き期待値=勝者の呪いは残る)。
ただしこの論点は**別の2つで閉じられます**。

(a) **東証の始値は板寄せの単一約定価格**。米国研究で勝者の呪いを起こす
    微細構造ノイズ (bid-ask bounce、薄い板での最終約定) は板寄せには
    原理的に存在しません。実際の出来高が1つの値段で交差します。
(b) ★ **実際にその値段で約定できています**。測定ノイズなら約定できません。
    - 別の同日決済戦略の実発注 (2026-08-13): 4/4 で滑り +0.00%
    - kabu の OpeningPrice vs J-Quants 5分足の始値: 50/50 完全一致

**「測定ノイズだ」と「実際にその値段で建てられる」は両立しません。**

### 16.12.5 ⛔ 指数の穴は「年が丸ごと消える」形で出る

`^N225` に欠けている期間があると `beta` / `resid_gap` が NaN になり、
`ok` が False になって **その期間の銘柄行がパネルから全部消えます**。
エラーも警告も出ません。実測 (2026-08, yfinance の `^N225`):

```
2016  97,989 / 98,692 (99%)
2017  63,007 / 126,083 (50%)   ← β の120日ウィンドウで前後に広がる
2018       0 / 139,461 ( 0%)   ← 年が丸ごと消えた
2019  56,760 / 112,408 (50%)
2020 110,723 / 111,216 (100%)
```

⛔ **「指数の行が薄い年」を数えても捕まりません。** 行は存在します。
落ちているのは β / 残差の側です。検出は **年 × 除外理由の集計**
(`_DROP` / `_note_drop`) で、残った行が中央値の6割未満の年について
どの条件が食べたかを印字します。

回避は `--index-from-universe` (市場ファクターをユニバース自身から等加重で
作る。構造上穴が空かない)。⚠ 日経平均は株価加重なので β の水準が変わり、
`^N225` で出した数字と直接は比較できません。

### 16.12.7 ⛔ 順位カバー率の改善と α の低下は表と裏かもしれない

指数の定義を変えると、次の2つが**同じ向きに**動きます。

```
残差化が弱まる → resid_gap が生ギャップに近づく
  → 生の騰落率順位でのカバー率が上がる   (良く見える)
  → 市場調整の効果が減って α が下がる     (悪く見える)
```

極限では `resid_gap = 生ギャップ` でカバー率100%ですが、それは残差化を
捨てたのと同じです。**カバー率の改善を成果として報告する前に、α の低下と
同じ変更から出ていないかを必ず確かめること。**

さらに悪い可能性: **等加重ユニバース指数は暴落日に日経平均より深く下がる**
(小型株)。指数ギャップが大きくなると期待ギャップも大きくなり、
**resid_gap が小さくなって暴落日に発火しなくなります**。稼ぎ頭の日の発火が
消えれば、カバー率は自動的に上がります。

実測 (2011-03-14 の指数ギャップ): `^N225` -2.05% / 等加重 **-9.58%**。
4.7倍です。この日の発火が減っていないかは必ず確認すること。

**実測 (2026-08-29、両方の指数で1回ずつ)**

```
                        ^N225      等加重ユニバース
corr(残差, 生ギャップ)    0.8964  →  0.8125     残差化は **強まった**
std比 残差/生            0.8447  →  0.7953     同上
2011-03-14 の発火          40件  →     7件     暴落日の発火は **消えた**
その日の指数ギャップ       -2.05%  →   -9.58%
総発火件数                547件  →   653件     全体は減っていない
§12 N=30 カバー率         89.0%  →   97.2%
```

→ **「残差化が弱まってカバー率が上がった」は棄却**。corr も std比 も下がって
   いるので残差化は強まっています。**「暴落日の発火が消えた」が確定**。
   両仮説は corr の向きに逆の予測をするので、1回の実行で判別できました。

⛔⛔ **より重い問題: 近年の結論が指数定義で反転します。**

```
2023〜 の エッジ/コスト比    ^N225 3.73 (合格)  /  等加重 1.86 (落第)
2025年 の 日次bp            ^N225 67.6         /  等加重 3.2
```

件数はほぼ同じ (236 / 233件) なのに1件あたりグロスが半分です。
**同じ期間・同じユニバースで、市場ファクターの定義だけで合否が入れ替わる。**
この頑健性の欠如そのものが、実弾を出さない理由になります。

判別は §1 の「指数の効き」ブロック:
- `corr(残差ギャップ, 生ギャップ)` — 1.0 に近いほど残差化が効いていない
- `std比 残差/生` — 同上
- **発火が多い日 上位10** — 暴落日が消えていないか

### 16.13 ⛔ 年別表は 発火日数 とセットでしか読めない

発火日数は年で3倍違います (2007-2022 は年3〜26日、2023-2026 は年23〜47日)。
**+209bp (2014年・17日) は17日の平均で、ほぼノイズです。**
一方 +50.78bp (2024年・47日) は3倍の標本で測っています。

→ 「昔は良くて最近落ちた」ではなく **「昔の数字が信用できないだけ」**
   の可能性が高い。結論 (劣化未検出) は変わりませんが理由が変わります。

`_year_table` は 発火日数 / 発火率‰ (取引数 ÷ ユニバース銘柄日数) /
|gap| 中央値 / ATR 中央値 を必ず出します。発火率が横ばいなら発火数の増加は
母集団の成長で説明できます。差の検定は **取引数で重み付けした版**も併記
(3日の年と47日の年を同じ重みで扱わない)。

**計画値には全期間の 66.03bp ではなく、標本の厚い近年の水準
(2023-2026 = 150日 / +52.11bp) を使ってください。52bp が保守側の計画値です。**

⚠ 「下位10日に2026年が3件、確率2.1%」は結果を見てから作った検定なので
   p 値として読めません。「レジーム崩壊とは判断しない」が正しい扱いです。

### 16.13.5 ★★ N の母集団の基準値 — 数字を引用するときは必ずここと照合する

```
全期間   20,729 取引 / 発火 2,613日 / 2015-02〜2026-08
TRAIN     9,666 取引 / 2015-02〜2020-08
TEST     11,063 取引 / 2020-09〜2026-08
1件あたり建値 約2,652円
```

⛔ **呼値は固定しないこと。** 「3,000円未満で1円」は**銘柄区分を無視した記述**
です。TOPIX100 構成銘柄には刻みの細かい区分があり、建値2,652円で 0.5円 の
ことがあります。**N は「売買代金 上位50」で選ぶので TOPIX100 が最も濃く
入ります。**

```
建値2,652円 / TOPIX100     呼値 0.5円 → 7.5bp = 約4ティック
建値2,652円 / それ以外     呼値 1円   → 7.5bp = 約2ティック
```

⚠ **この対応表も未確認です。主張として使わないこと。**
→ 実務は変わりません。`.\norder` に **その銘柄の呼値を記録**すれば、
想定に依存せず出ます。「許容2ティック」は下限の可能性があります。

⛔ **これと桁が合わない件数の表は N ではありません。**
実例: `--sweep-barrier` の **167,880銘柄日** (watch50 も ret1 も未適用)。

★ **この失敗は「相手に件数を聞く」では防げませんでした。相手がいなかったからです。**
167,880 と 11,063 は**同じ文書の別の節に並んで載っていて、3人とも割り算を
しませんでした**。だから防御線はコード側に置きます:
`nrule_worst_days._ref_check()` が実行のたびに比を計算し、1.5倍以上ずれたら
「成績を論じる前に仕様差を特定すること」と印字します。

**引用の時点で弾く。聞くのはその後。**

### 16.14 N ルール (固定%) との突き合わせ — `--nrule`

別セッションの `analyze_gap_edge.py` が回している N ルールを、こちらの
エンジンで表現して独立実装として突き合わせます。

```
前夜   前日リターン >= +1.753%   ⛔ 1.75% ではない。丸めない
       建値 1,000〜6,000円 / 売買代金20日平均の上位50銘柄
09:00  始値が前日終値の +100bp 以上 → 空売り
決済   その日の終値。損切りも利確も置かない
サイズ 予算400万 / 100株単位 / 1日の件数上限なし / 重複制限なし
手数料 0 / 発注順 |ギャップ|降順 / 片道 4.4bp (呼値1円 ÷ 建値2,257円)
鏡像 = 符号を全部反転 (前日 -1.753% 以下 → 始値 -100bp 以下 → 買い → 引け)
```

```
python gap_reversal_daily.py --nrule
```

`--nrule` は候補行の keep 条件を広げます (通常は ATR 正規化ベースなので、
生%で発火する行が落ちる)。出力は N / 鏡像それぞれの 年別表・差の検定・裾と、
**こちらの買い側との重なり** (件数・日・1件あたり)。

⚠ こちらは bp ベース、向こうは円ベースなので直接は一致しません。
  **一致を見るのは「発火日数」と「符号・順位のパターン」**。
  発火日数が大きく違ったら、そこで止めて仕様差を特定すること。

⛔ **単月を見て判断しないこと。** N の月次σ は月平均の1.7倍、プラス月は75%。
   見るのは「直近12〜24ヶ月が体系的に落ちているか」の1点だけです。

⛔ **参照値の訂正 (2026-08-29)。** N の TEST は **月 66,903円 / t=4.08** です。
   以前記録していた「月 89,364円 / t=5.04」は2つの実行が混成された誤りで、
   同じ表の中で t と 月平均÷σ が数学的に一致していませんでした。
   独立実装と保存台帳の両方が 66,900円前後で一致しています。

⛔ **件数が合わないうちは成績を論じないこと。** 参照値は TRAIN 9,670件 /
   67ヶ月 = **144件/月**、独立実装は 150〜190件/月。`--nrule` はこれに
   届いているかを**最初に**見ます。`nrule_report` は発火までの漏斗
   (パネル候補行 → 上位50位 → 前日条件 → ギャップ条件) と年別件数を
   必ず印字するので、どの段で落ちたかで仕様差を特定できます。

⛔ **「直近は OOS だから信じられる」とは書かない。** N の TEST (2020-09〜) は
   ①複数回見ている ②現象自体を全期間を見て思いついた ③別分析では split が
   2016-01-01 で直近月が複数の TEST に入る、の3点で純度が落ちています。
   正しくは **「TRAIN では一度も使っていない期間」**。真にクリーンなのは
   仕様を凍結した 2026-08-27 以降だけ (2日) で、使えません。

### 18.7 ランキング観測 (`kabu_ranking_log.py`)

⛔ **発注機能はありません。** GET は `/kabusapi/ranking` だけです。

```
python kabu_ranking_log.py            # "1"x"TP" / "2"x"TP" / "1"x"ALL" / "2"x"ALL"
python kabu_ranking_log.py --probe    # 件数指定/ページングの引数があるか
```

```
Type              "1"=値上がり率 "2"=値下がり率 "3"=売買高 "4"=売買代金
                  "5"=TICK回数 "6"=急上昇 "7"=急降下   ← すべて文字列
ExchangeDivision  "ALL" / "T" / "TP" / "TS" / "TG" / "M"
返り値            {"Ranking": [...]} **50件** (2026-08-30 実測)

⛔ **「通常 最大30件」は誤りでした。** 実測で **50件**、かつ
`Count` / `Limit` / `Size` / `Page` / `Offset` の5種すべてで変わりません
(50件固定の上限)。私が仕様説明をそのまま記録したもので、未確認でした。
→ **§12 の判定は N=30 ではなく N=50 の行で読みます。**
```

- 保存は `forward_records/ranking_<date>.jsonl` に **生 JSON を追記** (列を作らない)
- `req_ts` / `resp_ts` の両方を打ち、失敗・タイムアウト・429 も `ok:false` で1行残す
- **ALL を必ず取ること。** 2回分の追加コストしかなく、後から絶対に取れません
- ⛔ **Day 1 は ranking だけ。`/board` を混ぜない。** 銘柄登録は50件上限、
  登録直後の初回読み取りが 40〜140秒。過去に同じ朝で 429 が積み上がって
  発注が危うくなった実例があります。kabu の有効トークンは1つなので、
  他プロセスと同時に走らせないこと
- パスワードは `KABU_API_PASSWORD_PROD` (無ければ `KABU_API_PASSWORD`)。
  ⛔ コードに埋め込まないこと
- `gap_forward_record` は kabu を使わないので並走して構いません

#### ETF の初約定時刻 (`--etf`)

```
python kabu_ranking_log.py --etf --repeat 8 --interval 180   # 08:55〜09:20
```

⛔ **「ETF は 09:00 に必ず確定する」は成立しません。** ETF も板寄せなので、
注文不均衡があれば特別気配で寄りません。ETF を市場ファクターに使える
必要条件は:

```
ETF の初約定時刻 ＋ 計算・発注時間 ＜ 対象銘柄の板寄せ成立時刻
```

履歴では測れないので、朝に `/board` の `OpeningPriceTime` を記録します。
登録は **1321 と 1306 の2件だけ** (上限50件・429 の危険があるので発火候補の
板は登録しない)。

⚠ **1306 は 1321 の代替ではなく頑健性の確認**として扱うこと。成績の良い方を
選ぶと、指数の選択そのものが探索になります。

⚠ ETF を指数に使うなら **分配落ちと分割の補正が必須**。1321 は毎年7月8日、
1306 は7月10日が分配基準日、1306 は 2026年4月に分割。β も調整済み
リターンで推定し直す必要があります。

#### ⛔ 等加重ユニバース指数は 09:00 に確定しない

`gap_reversal_daily.universe_index()` は全1,384銘柄の**当日公式始値**の平均で
`idx_gap` を作ります。板寄せに寄成を出す判断は板寄せ成立**前**に済ませる
必要があるので、この戦略にとっては**実質的に先読み**です。
**59.58bp / 97.2% / 1.86 は執行可能な数字ではありません。**

⛔ `^N225` に戻る道も塞がっています (2017-07〜2019-07 の2年欠落)。
**両方とも使えない**のが現状で、ETF の初約定時刻の観測はこの穴を埋める
ためのものです。

### 16.15 N の大負け日 — 集中ではなく共変動 (2026-08-29)

`nrule_worst_days.py` で解剖しました。**指数を使いません** (N ルールは生%の
条件だけで決まるので β も残差も不要)。

**結論: 大負けは1〜2銘柄の集中ではなく、全銘柄の共変動です。**

```
大負け上位20日の 負け比率     N 78% / 鏡像 93%
最悪1件が負けの合計に占める割合  実測 42.7% / 偶然でも 34.0% (n≈8)
相関 (過去60日リターン)        大負け日 0.33-0.57 / 大勝ち日 0.24-0.72  差なし
N225 日中%                  N の大負け日はほぼ全部プラス、大勝ち日はマイナス
                            鏡像は完全に逆 (-8.86 -4.75 -10.75 ...)
```

⛔ **帰無仮説を 1/n にしないこと。** n 個の独立な損失の最大値は偶然でも
平均を大きく超えます (指数分布なら `E[max/合計] = H_n/n`。n=5 で 45.7%、
n=13 で 24.5%)。1/n と比べると**何を測っても「集中」に見えます**。
一度これで誤り、ラベルだけ直して式を直し忘れた実行を出しました。

### 16.15.1 潰れた系統 (13・14番目)

**13. 金額均等 / ATR均等** — §18.30 に「未検証」とあった件。**否定で決着**。

```
月平均/σ        100株固定   金額均等   ATR均等
N   TRAIN         0.248     0.203     0.179
N   TEST          0.081     0.030    -0.003
鏡像 TRAIN         0.234     0.003     0.014
最悪の日 (N)     -98,237  -806,641  -806,641   ← 8倍
```

2単元以上が60〜67%なので実際に効いた上での結果。総額は上がるが σ が5〜6倍で、
**リスク調整後は全方式で悪化**。これはレバレッジであって分散ではありません。

**14. N と鏡像の同時建て** (同一銘柄の両建てとは別物)。両者の大負け日は
市場が自分と逆に日中動いた日で β の符号が逆なので、相殺を期待した。

```
                月平均/σ      最悪の日
TRAIN  鏡像       0.234      -272,431
       両側       0.329      -272,431   ← まったく同じ
TEST   鏡像       0.231      -182,315
       両側       0.215      -182,315   ← 同じ。しかも比率は悪化
発火日の重なり 26%
```

★ **最悪の日が片側と1円も違わない = その日に反対側が1件も建っていない。
相殺は起きていません。** これは月次相関のような間接証拠ではなく直接証明です。
全期間の 月平均/σ 改善 (0.234→0.290) は取引日が増えただけの
日をまたいだ分散で、**裾のリスクは1円も減っていません**。

### 16.15.2 サイズは ES で決める (`--risk`)

⛔ **観測された最悪の1日で決めないこと。** 将来の上限ではありません。

```
python nrule_worst_days.py --risk
```

下位1%/5% の Expected Shortfall、日ブロックのブートストラップによる
90%区間、平均日次÷|ES|、最悪5日/10日平均を、**強制持ち越しを含む版と
除く版の2本**で出します (持ち越しは日中の値幅制限の外に出るので別のテール)。

予算 = 許容できる1日の損失 ÷ |下位1% ES の信頼**下端**| × 現行400万。
点推定で決めると半分の確率で外れます。

### 16.15.3 ボラ回帰 — 判定して打ち切る

「σが高い日は建玉を落とす」。`--risk` が同時に出します。判定は2つだけ:

- (a) `|日次損益| ~ 予測ボラ` の R² — 0.1 も無ければボラを予測できていない
- (b) 予測ボラ5分位別の **平均が比例するか、フラットか**
  - 比例 → サイズ変更は中立。**打ち切り**
  - フラット (Q5/Q1 の平均比 ≈ 1.0 で σ だけ増える) → 平均/|ES| が改善する

⛔ (b) がフラットでなければそこで終わり。深追いしないこと。

### 16.15.5 ⛔⛔ N は「小さく始める」ができない

これが 2026-08-29 でいちばん重い発見です。

```
件数分位   Q1(少) 平均/|ES| 0.001  →  Q5(多) 0.039   (39倍)
別測定: 件数上限を絞ると 負け月 18→24 に増え、最悪月も悪化 (§18.58③)
```

N は **100株固定なので、1銘柄あたりを小さくできません**。予算を減らす手段は
「1日に建てる銘柄数を減らす」だけです。ところが件数を減らすと
**リスク調整後は比例して下がるのではなく、悪化します**。

⛔ したがって「予算 = 許容できる1日の損失 ÷ |ES| × 400万」という式は
**誤りです**。この式は「小さくすれば比例して安全になる」を前提にしていますが、
N ではその前提が成り立ちません。正しい問いは
**「B* = 400万 を固定したとき、必要な資本はいくらか」**です。

### 16.15.6 ⛔ ボラ回帰の棄却理由を間違えないこと

```
(a) |日次損益| ~ 予測ボラ   R² = 0.000 (pred_vol) / 0.000 (atr_mean) / 0.060 (n_pre)
(b) 予測ボラ5分位の 平均/|ES|   0.001 → 0.039
```

⛔ 正しい理由: **ボラは予測できていない (R² 0.000〜0.060)。効いていたのは
件数だけで、平均は n に比例・σ は √n でしか増えないので、平均/σ の改善は
ただの分散効果**。高ボラ日にエッジがあるのではありません。

⛔ 誤った言い方: 「σが高い日に建玉を落とすと成績が悪化する」。
これは円損益を使ったことによる交絡 (建玉額と件数を説明してしまう) を
エッジと読み違えた形です。**円損益ではなく総建玉で割った日次収益で
測るべき**でした。結論 (打ち切り) は変わりませんが、理由が違います。

⛔ また `Q5/Q1 の平均比` は Q1 の平均がほぼゼロだと発散します
(実データで 234.71倍)。比ではなく **平均/σ と 平均/|ES| の推移**で読むこと。

### 16.15.7 ES はこのスクリプトの数字を使わない

`nrule_worst_days.py --risk` の母集団は参照実装と一致していません
(件数 110件/月 vs 144〜190、発火日 月11日 vs 約20日、月額 約1万円 vs 30,638円、
期間 2003-08〜 vs 2015-02〜)。**比較には十分でも、サイズ決定には母集団の
一致が必須**です。ES は台帳とゲート一致した実装で出すこと。

### 16.15.4 ⛔ N の突き合わせは終了

件数が 37件/月 と参照値 144件/月 で4倍違った原因は
**「売買代金 上位50位をどの母集団で数えるか」**でした。

```
rank-first  (全銘柄を売買代金で並べて上位50 → 条件を当てる)     37 件/月
filter-first(前日条件を満たす銘柄の中で上位50)               110 件/月
```

残り 1.3倍は期間とユニバースの差 (こちら 2003-08〜 / 参照 2015-02〜) ですが、
**追いません**。別の独立実装が台帳と 20,406取引 全件一致しており、
N の実装は2つで検証済みです。`--nrule` は3つ目で追加の情報がありません。

### 16.15.8 ⛔ 先読みだった: 20日平均売買代金に当日を含めていた

2026-08-29 の検査で発見。`rolling(20).mean()` は**当日を含みます**。
監視枠 (上位50) は**前夜に選ぶ**ので、当日の出来高は使えません。

```python
# ⛔ 誤り
d["turnover"] = (d["close"] * d["volume"]).rolling(20).mean()
# ✅ 正しい
d["turnover"] = (d["close"] * d["volume"]).rolling(20).mean().shift(1)
```

影響したのは `gap_reversal_daily.py` と `nrule_worst_days.py` の両方。
**§11「前夜の売買代金 上位N位」が、実際には当日の出来高を見ていました。**
§7 の上位13件フィルタと `MIN_TURNOVER` にも効きます (発火が中央1件/日なので
上位13件はほとんど拘束せず、影響は小さいはず)。

⚠ `gap_forward_record.py` は影響を受けません。`prepare` は前夜に走り、
最終バーが D-1 なので `.iloc[-1]` は正しく D-1 までの平均です。

**教訓: rolling 窓を「前夜に分かる量」として使うときは必ず shift(1)。**
列名に `_prev` を付けるなどして、選択に使う量と当日の量を混ぜないこと。

★ **同じ週末に、3人が独立に同型の欠陥を見つけました。**

```
実装側スクリプト  rolling(20).mean() が当日を含む       (検査で指摘 → 修正)
本体 gap_reversal_daily.py:501  同じもの               (自己発見 → 修正)
本体 analyze_gap_edge.py:623    watch50 の候補プールが
                                当日始値を要求している   (検査役が発見)
```

**3件とも「前夜に選ぶものを、当日のデータで選んでいた」という同型です。**
1件見つかったら、**同じ性質の量を全部洗うこと**。単発の bug ではなく
コードの書き癖なので、必ず他にもあります。

判定に使う量は3種類に分けて考える:

| 種類 | 使ってよい情報 | 例 |
|---|---|---|
| 前夜に選ぶ | D-1 まで | 監視枠、価格帯、ATR、β |
| 09:00 に判定 | D の始値まで | ギャップ、残差ギャップ |
| 事後の評価のみ | D の終値以降 | o2c、持ち越し、next_open |

**列を作る時点でどれに属するかを決め、混ぜないこと。**

### 16.15.9 参照実装との残る仕様差 (追わないが記録する)

- **価格帯の判定**: こちらは前日終値、本体は**当日始値**。前夜の選択には
  前日終値しか使えないので、こちらの方が執行に忠実。ただし母集団が変わる
- **こちらだけにある追加条件**: ATR の有効性、日次変動 30% 以内
  (データ破損の除外)。本体には無いので、こちらの方が発火が少なくなる

件数差 (110 vs 144〜190件/月) の残りは、これと期間・ユニバースの差です。
**追いません** (§16.15.4)。

### 16.15.10 損切りは棄却。ただし過程に2つの教訓がある (2026-08-30)

**0.1ATR の個別損切りは棄却。** 滑り0でも TEST 差 −37.88円/件、分岐点 −1.75bp。
TRAIN の 0.02〜0.10 も全点マイナスで、山も崖もありません。
利確・早期決済・ポートフォリオ損切りも棄却済み。**N は損切りなしのまま。**

#### ★ 実装間で比較してよいのは Δ と発動率だけ

```
Δ = (発動したトレードを引けまで持った場合) − (損切値で切った場合)
  = −(現行との差) ÷ 発動率
```

円/件の差もベースラインも母集団に依存しますが、Δ はほぼ依存しません。
2つの実装が符号ごと食い違ったとき、**この1つの量に還元したことで争点が
特定できました**。手順としては正しかった。

#### ⛔ しかし原因の仮説は外れた — 先に件数を聞くべきだった

私は「発動判定の解像度が違う (日足の高値 vs バーの終値)」と推定しましたが、
**両実装とも日足の高値**でした。真因は**母集団**で、参照側の
`--sweep-barrier` が watch50 を掛けていなかった (167,880銘柄日 vs 正しい N の
9,666取引 = 17倍)。

⛔ **私は同じセッションで、相手の ES を「母集団が一致していないから使えない」と
却下しています。その確認を、自分が受け取った表に対してはしませんでした。**
**件数を1回聞けば済んでいました。** 数字から機構を組み立てる前に、
その数字がどの母集団のものかを聞くこと。

#### ★ 残る知見: 狭いバリアは履歴では解像できない

**0.25% は建値2,500円で 2.5ティック。実際の逆指値は1ティックの上振れでも
発動します。** 日足の高値でも5分足の高値でも足りず、板の1約定単位でしか
決まりません。

→ **建値の 0.5% より狭いバリアを過去データで検証するときは、この限界を
先に宣言すること。** 緩い損切り (sm>=0.5 ≒ 12ティック) は解像でき、
そこでは両実装が一致しています (どちらも「ほぼ効果なし」)。

#### ✅ 利確は正しい母集団で再測定済み — 棄却確定

正しい N で測り直したところ、**壊れた母集団のときより5倍強く出ました**
(tm0.3 で −43円 → **−242.55円/件**)。形は同じで、狭い側の害が深い。
**実用域すべてで悪化。結論は確定です。**

⚠ 私は「指値だから結論は変わらないだろう」を根拠が弱いと書きましたが、
実際には結論は変わらず、**大きさだけが5倍になりました**。慎重側に倒したのは
正しかったが、当たったわけではありません。

#### (参考) 旧記述: 利確の結論は未確認のまま

利確8段が「単調に害」という表も **同じ壊れた母集団**です。損切りは母集団を
直したら符号が反転したので、⛔ **「指値だから結論は変わらないだろう」を
確認済みとして記録しないこと。** 優先度は低いが、未確認は未確認。

### 16.16 ⛔ 事前宣言: ETF ゲートが落ちたときに何をするか (2026-08-29 記載)

⛔ **これは月曜の観測より前に書いています。** 結果を見てから決めると
都合の良い後付けになるためです。

**採るのは (a) `--no-index` = 生ギャップ。** 理由は1つだけ:

> 対象銘柄の生ギャップは、**その銘柄が寄った瞬間に確定する**。指数を一切
> 必要としないので、09:00 問題が構造的に消える。3つの候補のうち、
> 執行可能性が保証されるのはこれだけ。

⚠ **「残差化を捨てると成績が落ちるはず」は、我々のデータでは支持されていません。**

```
§13 持越後      ^N225 66.03bp/t=3.14  等加重 59.58/3.40  --no-index 70.96/3.72
```

`--no-index` が最も高い。ただし期間が違う (2003-08〜 vs 2007-03〜) ので
**期間を揃えて確認する**必要があります。

**事前に判定条件を書きます:**

- 期間を揃えた比較で、生ギャップ版の §13 持越後 **t が 3.0 を下回ったら (a) も落第**。
  そのとき初めて (b) に行く
- ⛔ **生ギャップ版では §12 のカバー率が 100% 近くになりますが、それは
  成果ではありません。** シグナルが生の騰落率そのものになるので当然です
  (§16.12.7 の「カバー率と α は表と裏」の極限)。カバー率の改善を
  根拠に使わないこと

**(b) 前夜に確定する市場代理を、いま選ばない理由:**

日経225先物のナイトセッション終値 vs 前日大引け は 09:00 前に確定し、
市場の「期待ギャップ」として筋の良い候補です。それでも後回しにするのは:

1. `.rsi2_cache` に無く、データ取得から始まる
2. 控除の意味が「**実現した**市場成分」から「**期待された**市場成分」に
   変わるので、これまでの全ての数字と比較不能になる

**(a) が落ちてから着手するのが安い。**

### 16.16.3 ★ 指数の決着 — ETF を待たずに切り分けられます (`--index-top-n`)

⛔ 未決だったのは「^N225 と等加重が 2023-2025 で 2倍食い違う原因」でした。
候補は2つ:

```
(a) 加重の違い      株価加重(大型株中心) vs 等加重(小型株が効く)
(b) データ品質      yfinance の ^N225 は2年欠落・始値ノイズの疑い
```

★ **流動性上位だけの等加重指数**で切り分けられます。

```
python gap_reversal_daily.py --prev-thr 0.0 --gap-thr 3.0 --side long \
       --index-top-n 100 --start 2007-03-26
```

| 結果 | 意味 |
|---|---|
| **^N225 に近い数字** | 原因は **加重**。等加重は小型株を拾いすぎている |
| **全銘柄等加重に近い** | 原因は **^N225 のデータ品質**。等加重の側が正しい |

⚠ 銘柄の選択は **前日までの売買代金** (shift(1) 済み) なので前夜に確定します。

★★ そして **これは執行可能な指数の候補でもあります。** 全銘柄の等加重は
最後の1銘柄が寄るまで確定しませんが、**流動性上位100銘柄なら 09:00 に寄る**
見込みです。⛔ **未検証** — 分足 (2024-07 以降) で確認できます。
通れば **ETF を待たずに指数の問題が片付きます。**

⚠ ただし「流動性上位は 09:00 に寄る」は私の推測です。測ったのは
「大きく動いた銘柄は遅く寄る」(corr +0.590) であって、
**「流動性が高い銘柄は早く寄る」は測っていません**。
借りた機構を確認せずに使わないこと (§16.18.1 と同じ形)。

### 16.16.2 ⛔ 事前宣言: ETF ゲートが通っても、等加重の数字は戻りません

観測の前に、3つの場合それぞれで何をするかを書きます。

⚠ **まず前提の訂正**: 「対象銘柄の多くも 09:00:00 に寄るのでゲートは
構造的に通らない」は、**ユニバース全体には当てはまりますが、私の
シグナル母集団には当てはまりません**。§16.9 の実測:

```
09:00 ちょうどに寄った   5 / 363 件 (1.4%)
寄りの遅れ              中央 14分 / 90%点 26分
```

**私のシグナルは |ギャップ| >= 3ATR で選ぶので、まさに特別気配になる銘柄です。**
98.6% が遅れて寄るので、**ゲートは構造的に有利**です。

⛔⛔ **しかし、通っても等加重の数字は戻りません。**

```
1321 / 1306 は 日経225 / TOPIX を追う ETF
→ 得られるのは **^N225 型の市場ファクター**
→ **等加重ユニバース指数に対応する ETF は存在しない**
```

つまり **59.58bp / 97.9% / 1.86 (等加重) は、ETF が通っても執行可能に
なりません。** 復活しうるのは **66.03bp / t=3.14 (^N225 型)** の枝だけです。

★ ただしそれには価値があります。私が `^N225` を退けた理由の一部は
**データ品質** (2017-07〜2019-07 の2年欠落、|指数ギャップ| 中央値が
等加重より大きく始値ノイズを疑わせる) でした。**ETF の実約定始値なら
その問題は消えます。**

**場合分け (観測前に確定):**

| 観測結果 | 次の一手 |
|---|---|
| **ETF が 09:00 台に寄り、対象より十分早い** | **^N225 型を ETF の実データで作り直す。** ⚠ 分配落ち (1321:7/8, 1306:7/10) と 1306 の2026年4月分割の補正が必須。β も調整済みリターンで再推定。→ その上で §13 を測り直す |
| **ETF が特別気配で遅れる / 対象と差が無い** | **`--no-index` (生ギャップ)。** §16.16 の事前宣言どおり、期間を揃えて §13 持越後 t が 3.0 を超えるかを見る (`--start` 実装済み) |
| **両方 09:00:00 (差が測定分解能以下)** | 上と同じ扱い。**「差が無い」は「使えない」と読む** |

⛔ **どの枝でも「等加重で測った数字」は使いません。** 09:00 に確定しない
以上、あれは執行可能な数字ではありません (§18.7)。

### 16.16.1 月曜の判定は非対称

**正常な1日は「配管テスト」であって、実行可能性の証拠にはなりません。**
逆に、正常日でも次のどれかが出れば **その場で棄却**できます:

- 未寄り銘柄がランキングに載らない
- ETF の始値の受信が遅い
- 注文準備が対象銘柄の板寄せに間に合わない

→ **通っても「まだ生きている」だけ。落ちたら確定で死に。**

⛔ 判定式は **公式の `OpeningPriceTime` 同士の差ではありません**:

```
ETF始値を初めて受信した時刻 (resp_ts) ＋ 計算・発注時間 ＋ 安全余裕
  ＜ 対象銘柄の板寄せ成立時刻
```

API の配信遅延とこちらの処理時間を落とさないため、`req_ts` / `resp_ts` の
両方を記録します。

### 16.17 ⛔⛔ 再発パターン: 「衛生的な名前の除外条件」が出口を読む

2026-08-30、**別々のコード・別々の担当・同じ日**に同じクラスの欠陥が出ました。

```
独立台帳    値幅制限検査が日足を偽陽性で除外 → 20日平均売買代金に穴
            → 上位50の境界が動く
こちら      sane = ... & (o2c.abs() <= 0.30)
            o2c は 始値→終値 = **損益そのもの**
```

**1,000円の株の値幅制限は ±300円 = ±30%。** つまり `|o2c| > 30%` は破損とは
限らず、**ストップ安の日が「破損」として消えます**。それは §10 で
「決済できない恐れ」として数えた日そのものです。

共通の形は3つ:

1. **名目が衛生的なので疑われない** (「データ破損の除外」「値幅制限の検査」)
2. **閾値が 30% のように丸い** — 実際の制度上の境界と一致していない
3. **落ちた件数を印字していない** — 存在に気づけない

★ 対策は2枚:

- **除外条件を「建てる前に分かる量」と「出口の量」に分け、出口側の件数を
  必ず印字する** (`sane_pre` / `sane_out`)
- **シャッフル検査** (`--shuffle-test`)。出口の列を NaN でない行の中で
  シャッフルし、**選ばれた (date, symbol) の集合が変わらないこと**を示す。
  ⚠ 比べるのは pnl ではなく集合。pnl は当然変わるので検査にならない
  ⚠ アサーションより強い: 条件が増えても効き続ける

実測: パネル構築 **後** はシャッフル3試行とも差0で通過。**漏れは構築時の
`sane` にありました。** 検査は層ごとに要ります。

### 16.17.1 ⛔ commit 前は import を通す。ast.parse では足りない

同じ日に、**定義の入っていないコードを push しました。** 置換スクリプトが
assertion で止まって何も書かず、次のスクリプトが参照側だけを書き、
`ast.parse` は通ったので commit しました。**構文は正しく、名前が無い**状態です。

```
python -c "import <module>"     ← モジュール先頭の NameError を即座に捕まえる
```

⚠ そして self-test が `build_panel` を通っていなかったので素通りしました。
**通らない経路は壊れていても気づけません。** `_smoke_build_panel()` を
self-test から必ず呼ぶようにしました (2枚目の網)。

### 16.18 ★ 寄り順で予算を配ると、最良のトレードから枠が埋まる (2026-08-31)

**レポートは「全員が寄るのを待った」順で予算を配り、ライブは「寄った順」で配ります。**
そして **遅く寄るのは大きく動いた銘柄** — `corr(|ギャップ|, 遅れ) = +0.590`
(§16.9、ホールドアウト 363件/181日 の実測)。

⛔ ギャップ幅の分位別 α は **単調増加** (§4) なので、先着順に埋めると
**分布の右端だけを系統的に捨てます**。ランダムな劣化ではありません。

N での確定値 (別セッションの独立実装):

```
グロス (レポート表示)        +24.5bp
順序控除 (資金加重・immediate) −2.6bp
────────────────────────────────
                             +21.9bp   ← 秒単位の順序差・執行コスト控除前
実約定 (呼値・滑り)           ⛔ 未測定
```

⛔ **「N は +24.5bp」と単独で言わないこと。必ずこの鎖で示す。**

### 16.18.1 ★★ 符号が機構から予測される効果に、ゼロを置くのは楽観的

この控除の CI はゼロをまたぎます。それでも採るのが正しい理由:

| | |
|---|---|
| 通常の探索 | 符号が未知 → **有意でなければ採らない** |
| **この件** | **符号が機構から予測されている** → 点推定を捨ててゼロを置くのは
保守的ではなく **楽観的** |

★ そして `corr = +0.590` が、その論証を **議論から測定に変えた**ものです。

⛔ **ただし +0.590 は 残差ギャップ側の母集団の値です。** N の母集団
(前日 +1.753% × 売買代金上位50 × gap >= +100bp) では測っていません。
機構は一般的でも **大きさは母集団ごと**なので、N の控除の根拠にするには
N で測り直す必要があります。
⚠ 私は「測る前から方向は確定している」と書きましたが、それは
**自分の母集団の数字を別の戦略に転用した**もので、§16.15.10 で自己申告した
のと同じ形です。**機構は借りてよいが、大きさは借りられません。**

⚠ 「前半・後半とも負」「2変種とも負」は **4つの独立確認ではありません**
(同じデータの分割と、同じ量の2つの測り方)。**根拠は機構であって回数ではない。**

### 16.18.2 両建ての否定 — 2経路が別の理由で一致

```
A: 最悪の日が片側と1円も違わない = その日に反対側が1件も建っていない → 相殺しない
B: 投入 3,983,700円 (100%) = 鏡像が N の枠を奪っていた         → 置き換え
```

**別の機構なので、両方を残すこと。** 片方だけでは弱い。
⚠ A (私の `--both`) は 100株固定で予算を使い切っていなかったので、**B の
置き換えが見えていませんでした**。同じ結論でも見ている機構が違います。

ライブ順の鏡像は摩擦ゼロで **−3.10bp**。両建ては N 単独より **−617,527円**。
⛔ §18.56 の「σ削減31%」は **+6.67bp の脚**を前提にしていたので成立しません。

### 16.18.25 ⛔ 訂正: 「N は月0.2件しか拘束しない」は誤り

私は「N は月0.2件しか予算が拘束しない」と書きましたが、**これは §10 の
『ストップ安で決済できない件数 月0.21件』を予算拘束と取り違えたもの**です。

実際の N は:

```
必要資金       毎月 3,974,533 〜 3,999,100円 (予算400万に対してほぼ満額)
同時保有ピーク  最大 12〜17銘柄
発火           月 110〜190件
```

**N の予算は毎日拘束しています。** こちらの戦略 (中央 1件/日) より
**はるかに強く拘束します**。したがって寄り順の控除は、
**N の方が大きく出るのが自然**で、私の「こちらの方が大きく出る可能性」も
根拠が逆でした。

⚠ 数字を別の節から引くときは、**その数字が何を数えたものか**を確認すること。
「月0.2件」は決済不能の件数であって、予算拘束の頻度ではありません。

### 16.18.3 ⛔ こちらの戦略にも同じ穴がある (未測定)

§7 は「上位13件 (売買代金順)」と全件を比べて **同一** (95.96 vs 95.74) でした。
⛔ **しかしそれは売買代金順であって、寄った順ではありません。**
シグナルは中央 1件/日 なので普段は予算が拘束しませんが、
**暴落日 (2011-03-14 は 40件) では強く拘束します** — そしてそれは
ギャップが最大で寄りが最も遅い日です。**同じ控除がここにも要ります。**

---

## 17. 検査役セッションとの連携ルール

別セッションを検査役に立てて結果を突き合わせる場合の固定ルール。

### 17.1 結果には必ず出所を付ける

```
誰が / コード名 @ gitハッシュ / 条件 (コードが印字したもの) / 件数・期間・母集団
```

`gap_reversal_daily.py` と `gap_reversal_intraday.py` は **レポート冒頭で自動的に
印字します**。人間が書き足す運用にすると必ず忘れるので、コードに出させること。

```
出所: gap_reversal_daily.py @ c437092+dirty  /  実行 2026-08-29 02:28
ギャップ反転 検証レポート  [ATR単位]  前日=0.0  ギャップ=3.0  コスト=30.0bp 往復
期間 2007-05-24 〜 2026-08-27   シグナル 1,615 件 (0.4 件/日)
母集団 1,362 銘柄 / 4,110 営業日 / 候補 644,625 行  (株価 1,000〜6,000円, ...)
```

`+dirty` は未コミットの変更がある状態。**その結果は再現できません。**

⛔ 出所を確認せずに別セッションの数字を自分の集合の話として扱うと、議論が壊れます。
実例: 検査役の「2025-04 で グロスの 65%」は 1,121件 の集合の性質で、こちらの
479件 の集合では 2025-04 は上位3ヶ月にすら入っていませんでした。**そもそも別の
戦略を測っていた**のに、3往復それに費やしました。

### 17.2 やり取りは PR のコメントに集約する

貼り直さない。出力は長く、転記の過程で条件が落ちます。

### 17.3 「問題なし」は承認ではない

検査役が同じモデルなら、**同型の盲点は検出できません**。信用するのは
**数字を伴う欠陥の指摘だけ**です。

ただし数字が付いていても十分ではありません。**その数字が仮説を判別できるか**を
確かめること。実例: 「`entry_price` が `official_open` と 99.6% 一致」は本物の
数字でしたが、「実装事故」と「遅れて寄るので一致して当然」の**両方が等しく説明
できる**数字でした。判別には生の分足が要りました。

判別力のある検査の型:

- 予想と逆符号になるはずのプラセボ (§16.4 の (b)(c))
- 物理的に起こり得ない値の検出 (§16.5.5 の値幅制限)
- 同じ現象を別経路で測って一致するか (日足の始値 vs 分足の先頭バーの始値)
- 生データを直接見る (`--dump-bars`)

---

## 18. 前進記録と小ロット運用 (`gap_forward_record.py`)

**発注機能はありません。** 売買系のモジュールを import していません。
記録と集計だけを担当します。

### 18.1 前夜に確定できるもの

前日終値 / ATR20 / β / 前日方向 (同符号条件) は**すべて前夜に決まります**。
同符号条件があるので **各銘柄は上下どちらか片側しか発火しません**。
板の購読上限 (同時50銘柄) に対する候補を半分に絞れます。

```
python gap_forward_record.py prepare
```

`prepare` は3つの前提を強制します。**どれも翌朝の候補を誤らせるものです。**

1. **指数が銘柄と同じ日まで来ていること。** 指数が1日古いとその日の指数
   リターンが無く、`prev_z` が市場調整なしで計算されて `eligible_side` が
   誤ります (市場が2%下げた日に個別も2%下げた銘柄は残差ほぼゼロなのに、
   調整なしだと強い下げと判定される)。

   キャッシュに無い/古い場合は yfinance から取り直します。それでも足りない
   場合 (yfinance の ^N225 は1日遅れることがある)、**欠けた1日だけを
   ユニバースの等加重平均リターンで代用**し、何をしたかを表示します。
   日経平均は株価加重なので厳密には一致しません。**ニュースの騰落率と
   大きく食い違うなら `--strict-index` で止めて**指数を取り直してください。
2. **最終バーが最新の営業日であること。** 古い銘柄は `prev_close` が過去の値
   なので除外します (実データで30銘柄が該当)。候補の `asof` は必ず1種類に
   揃い、揃わなければ assert で落ちます。
3. **トリガー価格が値幅制限の内側にあること。** `3 × ATR × 前日終値` が
   制限値幅を超える高ボラ銘柄は**構造的に発火しません**。板の購読枠
   (同時50銘柄) を無駄にしないよう外します。

   実例: 5803.T 前日終値 5,340円 / ATR 7.86% → 必要 1,259円 に対し
   制限値幅 1,000円 で到達不能。5801.T も 必要 1,047円 / 制限 700円 で不能。

`forward_records/candidates_<date>.csv` に、銘柄ごとの
`prev_close / atr20_pct / beta / prev_z / eligible_side` と、
**指数ギャップ0を仮定したトリガー価格**、および朝の補正係数
(`idx_adj_per_1pct`) を書き出します。朝は

```
トリガー価格 = trigger_*_at_idx0 + idx_adj_per_1pct × (指数ギャップ%)
```

の一次式を計算するだけで済み、09:00 に重い処理をしません。

### 18.2 気配の記録 (別ツール)

証券会社のAPIに依存するので CSV で分離しています。仕様:

```
date, symbol, poll_time, quote_price, quote_kind, index_level
```

`quote_kind` は `quote` / `special_quote` / `traded`。1銘柄につき複数行
(1〜3分おき) を想定。

### 18.3 突合と判定

```
python gap_forward_record.py reconcile --quotes quotes_2026-09-01.csv
python gap_forward_record.py report
```

**判定は誤差の平均ではなく、発火判定の反転率と裾 (90/95%点)** です。
閾値は中央 約1,000bp なので、誤差が数百bpでも反転はほとんど起きないはず、
という仮説を検定します。

### 18.4 小ロットの実弾記録

```
python gap_forward_record.py live-init    # 雛形CSVを作る
python gap_forward_record.py live         # 集計
```

`forward_records/live_trades.csv` に1件1行。列は
`quote_time / quote_price / order_time / expected_price / official_open /
fill_price / fill_qty / order_qty / exit_time / exit_price / exit_qty / note`。

**測るのは損益ではなく執行です。**

| # | 見るもの | 意味 |
|---|---|---|
| 1 | 約定できたか / 一部約定 | 比例配分の有無 |
| 2 | **約定価格 と 始値 の差** | 板寄せに参加できていれば 0bp |
| 3 | 引けで決済できたか | ストップ張り付きの実害 |
| 4 | 損益 | 参考のみ。件数が統計にならない |

**【2】が全件0bpなら、特別気配の銘柄でも板寄せに参加できています。**
残る未知数はそこだけなので、数件で決着します。

⛔ `forward_records/` は `.gitignore` 済み (実弾の記録なので)。

### 18.5 発注は未実装

`kabu_token.py` はトークン取得のみで、発注は入っていません。
`FrontOrderType` の寄成コードなどを記憶で書くと実弾で事故るので、
**デモ口座 (18081) で公式仕様を突き合わせてから**実装します。

### 18.6 未検証のまま残しているもの

実弾のサイズを上げる前に片付ければ間に合う、という判断で保留:

- **廃止銘柄 (サバイバーシップ)** — 買い側の生死を決める。§16.6.35
- **気配 → 板寄せ価格の予測精度** — §18.3 で並行して測れる
- **決算の分離** — `--earnings`
