# 引き継ぎ資料 (2026-07-22 セッション)

lss (ロング銘柄ショート = 逆指値空売り・同日決済) の運用改善セッション。
発注→監視(watcher)→バックテスト整合の不具合を複数修正した。**次セッションはまず「未確認事項」から。**

- 作業ブランチ: `claude/implement-stop-order-session-7uBFu`
- 主要ファイル: `run_signals_holdout_all.py`, `nikkei_analysis.py`, `backtest_limit_entry.py`,
  `sameday5m_firsttouch.py`, `lss_exit_watcher.py`, `merge_lss_proposals.py`

---

## ★ 未確認事項 (最優先・ここから)

1. **約定モデル修正(v5+v6+v7)を daily レポートに反映**（残作業はこれだけ）
   - **v5**: 約定価格を `opens[ei]`(約定バー始値)→`opens[0]`(その日の始値) に修正。
   - **v6**: 寄り値を**日足始値優先**に。J-Quants5分足の寄りが稀に壊れる(例: ハードオフ2674の
     2026-07-22は実際2,869で寄ったのに5分足が2,819始まり)ため、信頼できる日足始値で寄りギャップ判定。
   - **v7**: **寄り約定(ギャップで始値約定=約定価格≠トリガー)時は約定バーから損切り/利確を判定**。
     建玉は寄りから開いているので約定バー内のヒットも計上。ザラ場約定は従来通り次バーから。
   - **debug_lss_bar.py で両方確認済み(v7動作OK)**:
     - ハードオフ2674: 日足始値2,869>トリガー2,862=ザラ場約定 → タイムカット**+2,230**(実+2,420) ✓
     - 資生堂4911: 日足始値3,200<トリガー3,213=寄り約定 → 約定バー#0で損切り**-3,042**(実も損切り) ✓
   - **残作業**: `.\daily` を最後まで走らせ(BTキャッシュ v5→**v7**で全再計算・初回遅い)、
     新しい `?v=` のHTMLで反映確認。ハードオフ=利益/資生堂=損切りに直り、全体が実態水準に。
   - 再確認診断: `python debug_lss_bar.py --symbol 4911 --date 2026-07-22 --trigger 3213 --stop 3224 --target 3103 --source local`
     (寄り約定=True・決済理由 stop・-3,042が出ればOK)

2. **フォワードテストで実運用乖離を測る** (§18.7)。約定モデルがv6で実態に一致したので、あとは
   実約定・実スリッページ・fill率を `forward_test.py --record` で貯めて劣化を定量化。

---

## このセッションで確定した運用方針

### 銘柄選定 = 3基準マージが最善 (安全)
- **12月+3月+6月 の union マージ**を lss 提案に採用。`daily.bat` が毎回 `merge_lss_proposals.py` で
  `lss_proposal_merged3.py` を再生成する。
- 根拠: 共通OOS(7月)で `merged3 +291,297` が単一3基準(12月+231k/3月+190k/6月+228k)を上回った。
  4ヶ月OOS(merged_old3=9/12/3)でも `+1,289,498 > 単一(+101万/+108万)`。
  効くのは「投票数」ではなく「プール拡大でBT降順がより高BTを拾える」から。
- **却下**: 投票数(多基準ラベル)でサイズや優先度を変えるのはOOSでエッジ未確認 → やらない。
- **却下**: 古い基準(12月以前)を足す → 劣化銘柄が混入(「12月だけ」の銘柄はIn-sampleですら負け)。
- 3ヶ月ごとに daily.bat のマージ行を更新(一番古いのを落とし最新を足す)。

### 日次コマンド
```
.\daily                          # long+short+lss(merged3), 発注サーバー起動, 6000円+無制限タブ
.\daily --no-long --no-short     # lssのみ=最速(発注だけ急ぐ朝)
.\lssbt <基準月>                  # lss単独調査(発注サーバーなし)。例 .\lssbt 2025-12
```

### 朝の監視
```
.\watch                          # watcherのみ(--all-dates 既定)。前夜発注→朝は監視だけ
```
- 前夜に発注 → 朝は `.\watch` だけ(寄り付き損切りに強い)。15:30まで止めない。
- **order server と watcher を同時起動しない**(kabuトークン1つで401競合)。

---

## このセッションの修正 (コミット順・重要)

| コミット | 内容 |
|---|---|
| daily を merged3(12/3/6 union)に切替+毎回再生成 | 選定を3基準マージに |
| ヘッダに MERGED_BASES 表示 | 「選定基準月: マージ 2025-12 / 2026-03 / 2026-06」 |
| 予算シミュに `LSS_BUDGET_MIN_BT`(既定30) | BT70集中の検証用。ただし400万タブは元々高BT集中なので実運用は不要 |
| 予算シミュ月次に `LSS_MONTH_FROM`(開始月) | マージ提案でOOS月だけ見る用 |
| **ENTRY_EXPIRE 1→0** | 注文有効を「翌営業日のみ(当日限り)」に。実運用の当日限り注文に一致。「1日後」約定が消える |
| **watcher を銘柄単位の合算決済に** | 200株(2建玉)が全部決済されない不具合を修正。kabu返済は銘柄単位で建玉自動選択・既存返済注文取消のため、建玉ごと決済すると片方のMOCが取消される |
| watcher が部分約定の残玉を監視継続 | `closed`永久スキップ廃止。20秒クールダウンで二重成行防止 |
| lss発注リストを1銘柄1行・数量=一致戦略数×100株 | 200株を1注文=1建玉で確実に。二重発注防止 |
| **v5: lss約定価格を opens[0](その日の始値)に** | ザラ場約定の過小評価を修正(上記★1) |
| debug_lss_bar.py 追加 | lss1トレードの5分足first-touchを1本ずつダンプ(検証用) |

---

## 解決した実運用トラブル (根本原因の整理)

### 「200株買ったのに100株しか決済されない」(ブックオフ/ハードオフ)
- **原因**: 同一銘柄2建玉で、建玉ごとにMOC/決済を出すと、kabuが発注時に銘柄単位で既存返済
  注文を取消すため、2つ目の発注が1つ目のMOCを取り消していた。
- **修正済**: watcher を銘柄単位の合算決済に(合計数量で1回だけ決済)。+ 発注も200株1注文に。

### 「ハードオフ/資生堂が損切りで終わった」
- **原因**: これらは**昨日ショート→MOC不具合で残玉が持ち越し→今日の寄りスパイクで板逆指値が発火**。
  バックテスト(新規同日)は正しく利益/タイムカット。実損失は持ち越しが原因。
- **ハードオフ**: バックテストは「タイムカット」(損切りではない)。「損切り」列はライン価格で結果ではない。
  約定バーの高値2,882は寄りの跳ね=約定前なのでBTは正しく無視(タイムカット)。
- **資生堂**: 5分足診断で確認済。損切り3,224に触れたのは#0(寄り)バーのみ。以降は3,224未満で目標
  3,103まで下落 → **新規同日なら+10,368が正しい**。実損失は持ち越しの板逆指値が寄りで刈られた。
- **修正済**: 銘柄単位合算決済+200株1注文で、同日に全株決済 → 持ち越しを無くした。

### BT約定値が実約定より辛い (ハードオフ 実+2,420 vs 旧BT-2,065)
- **原因1**: 約定価格に約定バー始値(トリガーより更に下)を使い、空売り約定を過度に安く計上。
  → **修正済(v5)**: opens[0](その日の始値)基準に。
- **原因2**: J-Quants5分足の寄りが稀に壊れる(ハードオフ07-22は実2,869なのに5分足2,819始まり)。
  → **修正済(v6)**: 寄り値を日足始値(yfinance)優先に。無ければ5分足opens[0]フォールバック。
- **原因3**: 寄り約定(ギャップ始値約定)なのに約定バーの損切りを見逃していた(資生堂: 日足始値3,200
  <トリガー3,213で始値約定→約定バー#0で損切り3,224ヒットなのに利益判定していた)。
  → **修正済(v7)**: 約定価格≠トリガー(ギャップ約定)なら約定バーから損切り/利確を判定。
- **確認済(debug_lss_bar)**: ハードオフ -2,065→+2,230(実+2,420) / 資生堂 誤利益→-3,042(実も損切り)。

---

## 打ち切った調査 (再開不要)

- **日次の市場タイミング(先物/日経ギャップでロング/ショート切替)** = 打ち切り。
  `test_day_market.py`(133日OOS)で寄り前指標とlss日次損益は無相関(むしろ弱プラス=仮説の逆)。
  `test_gap_filter.py` も同結論。**日で選ぶ効果なし**。
- **投票数(多基準ラベル)でサイズアップ** = 打ち切り。`analyze_srcbase.py` でOOSではエッジ消失
  (7月: 3基準PF0.58≒1基準0.59、非単調=ノイズ)。100株固定のまま。
- **寄りからN分待って約定** = 打ち切り。`test_intraday_delay.py` で検証。7ヶ月OOS(2025-12基準)で
  10分/15分待ちは即約定より明確に悪化(10分 -296万 vs 即 -206万)、ISは即が圧勝(PF2.03)。
  資生堂の「5分待てば利益」は後知恵で、平均では即約定が最良。**寄り即約定のまま**。
  (7月単月OOSだけ10分が勝ったが7ヶ月では否定=月固有ノイズ)。

---

## 調査用スクリプト (このセッションで追加)

- `debug_lss_bar.py` — lss1トレードの5分足first-touchダンプ(約定バー・損切/利確タッチを可視化)
- `analyze_srcbase.py` — マージ提案の基準月ラベル別にlss損益分解(`--oos-from YYYY-MM`)
- `test_day_market.py` — lss日次損益 vs 寄り前市場指標(先物/米株/ドル円)の相関
- `test_gap_filter.py` — 日経寄りギャップで上げ日ショート見送りフィルター検証
- `merge_lss_proposals.py` — lss提案を union/vote マージ(SOURCE_BASES/MERGED_BASES出力)
- `lssbt.bat` — lss単独・最小計算で基準月ごとにHTML生成(発注サーバーなし)
- `watch.bat` — 朝のwatcherのみ起動(--all-dates既定)

---

## 環境変数メモ

- `LSS_BUDGET_MAN`(万,既定400) — 予算シミュの資金
- `LSS_BUDGET_MIN_BT`(既定30) — 予算シミュのBT下限。70で全取引BT70に(実運用は不要)
- `LSS_MONTH_FROM`(YYYY-MM) — 予算シミュ月次の開始月(OOSだけ見る)
- `LSS_CLOSESTOP_RESWEEP` / `LSS_GUARD_ONLY` — 重い研究計算。daily.batが起動時にクリア
- 使ったら必ず `set XXX=` で戻す(付けっぱなしで次のdailyに影響)

---

## 注意 (次セッションで気をつける)

- **`git pull` はコード更新だけ**。レポートは `.\daily` 再実行で作り直さないと反映されない
  (HTMLの `?v=` タイムスタンプが変わっていなければ古いまま)。
- **`_BT_LOGIC_VER` を上げたら初回は全再計算で遅い**(v5に上げ済み)。
- **銘柄選定(scan_lss_universe)も同じ約定関数を使う**ので、v5で提案を作り直すとより正確
  (今のmerged3は旧モデルで選定。大崩れはしないが次回リフレッシュ時に作り直し推奨)。
- **.bat は ASCII-only**(日本語コメントはShift-JISで文字化けし実行失敗する)。
- モデルID `claude-opus-4-8` はコミット/PR/コードに書かない(チャットのみ)。
