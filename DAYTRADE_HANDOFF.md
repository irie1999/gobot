# デイトレ開発 引継ぎドキュメント

作成日: 2026-06-17  
作成者: スイングトレードセッション (gobot)

---

## 0. このドキュメントの目的

既存のスイングトレードシステム (gobot) の方法論をそのまま使い、
**デイトレード版を新セッションで開発する**ための引継ぎ資料です。

スイング版 gobot の詳細仕様は `/home/user/gobot/CLAUDE.md` に記載済み。
このドキュメントはデイトレ特有の差分・注意点・開始方法を補足します。

---

## 1. スイング版で「そのまま使える」考え方

以下の概念はデイトレでも変えずに適用できます。

| 概念 | スイング版での実装 | デイトレでも同じ |
|---|---|---|
| **WalkForward銘柄選定** | 12ヶ月訓練 / 6ヶ月テスト × 3fold | ✅ 同じfold設計でOK |
| **BTスコア** | 勝率×0.4 + PF/10×30 + 安定性×20 + 回数×10 | ✅ 計算式は流用可 |
| **ホールドアウトOOS検証** | HO30d〜HO180dで未来検証 | ✅ 概念は同じ |
| **保守モード / 積極モード** | conservative (tm=3.0) / aggressive (tm=1.5) | ✅ 同じ切替構造 |
| **ATRベースのエントリー/損切り/目標** | `order_p = close + atr * em` | ✅ ATRを分足ベースに変えるだけ |
| **スコア帯別実績分析** | BTスコア60以上が実運用基準 | ✅ 同じ閾値で始めてOK |

---

## 2. デイトレ用に変更が必要な箇所

### 2.1 データ粒度（最重要）

| 項目 | スイング版 | デイトレ版 |
|---|---|---|
| **バー粒度** | 日足 (yfinance daily) | 分足 (1min / 5min / 15min) |
| **ATR計算** | 日足ATR | 分足ATR (例: 15分足 × 14期間) |
| **シグナル判定** | 前日終値ベース | 当日寄り付き前 or 前日引け後のOHLCV |
| **データソース** | `yfinance.download(period="2y", interval="1d")` | `yfinance.download(period="60d", interval="5m")` |

分足データ注意点:
- yfinance の `interval="5m"` は直近60日分しか取得できない
- `interval="1m"` は直近7日のみ
- 本格的なバックテストには kabuステーション API or SBI 等の有料データが必要

### 2.2 保有時間・エントリー有効期間

```python
# スイング版 (backtest_limit_entry.py)
ENTRY_EXPIRE = 3     # 3営業日以内に約定しなければキャンセル
MAX_HOLD     = 15    # 最大15営業日 → タイムカット

# デイトレ版 (変更が必要)
ENTRY_EXPIRE = 0     # 当日中のみ有効 (15:30以降はキャンセル)
MAX_HOLD     = 1     # 同日中に決済 (もしくは時刻ベース管理)
```

### 2.3 損切りモード

```python
# スイング版
# stop_mode = "close"  ← 終値判定（引け成行）が既定
# → 理由: ザラ場のヒゲ刈り防止で成績が+59万円改善

# デイトレ版
# stop_mode = "intraday"  ← 必須
# → 当日中に全決済するので "close" モードは意味をなさない
```

### 2.4 エントリー価格の計算

```python
# スイング版: 前日終値を基準に逆指値
order_p = close_prev + atr_prev * entry_atr_mult  # em=0.0 なら close_prev そのまま

# デイトレ版: 当日寄り付き価格 or 直近N分高値を基準に設定
# 例: 5分足で「直前15分の高値ブレイク」をエントリー条件に
order_p = high_last_3bars + atr_5min * 0.1
```

### 2.5 スリッページ・手数料

```python
# スイング版 (backtest_limit_entry.py)
SLIPPAGE_STOP_PCT = 0.005   # 逆指値 ±0.5%
FEE_PCT_ONE_WAY   = 0.001   # 片道0.1%

# デイトレ版 (要変更)
SLIPPAGE_STOP_PCT = 0.002   # デイトレは流動性高いので小さめでOK
FEE_PCT_ONE_WAY   = 0.0003  # 信用デイトレなら 0.03% 程度 (証券会社依存)
# または日計り手数料無料のプランを想定 = 0.0
```

---

## 3. スイング版のシグナル戦略をデイトレに適用する方法

### 3.1 現行6戦略のデイトレ版アナログ

| スイング戦略 | 適用デイトレ版 |
|---|---|
| **MACD** (8,17,5) + 出来高スパイク + MA10 | 5分足MACD + 前場出来高急増 + 5分MA10 |
| **A7** ストキャス(14,3,3) + ATR + MA75 | 5分足ストキャス + 5分ATR + 30分MA (MA75の代替) |
| **RSI2** + MA200 + IBS | 5分足RSI(2) + 日足MA200 (銘柄フィルター) + 5分IBS |
| **DON** 15日高値ブレイク | 当日高値ブレイク or 前日高値ブレイク |
| **VOL** 5日高値 + 出来高1.5倍 | 当日前場 5本足高値 + 出来高スパイク |
| **MOM** ROC(10)>3% + MA25>MA75 | 5分ROC(5)>0.5% + 5分MA25>5分MA75 |

### 3.2 推奨: まず「前日終値ブレイク」戦略から始める

スイング版の em=0.0（前日終値で逆指値）と同等の、シンプルなデイトレエントリー:

```python
# 条件: 前日終値を当日寄り付き後に超えた最初の5分足
entry_trigger = close_yesterday
# 損切り: 前日終値 - 1ATR (5分足ATR × スケール係数)
stop_price = entry_trigger - atr_5min * 1.5
# 目標: 前日終値 + 2ATR
target_price = entry_trigger + atr_5min * 3.0
```

これならスイング版の論理構造（逆指値エントリー + ATRベースのR倍数）を
そのまま継承できます。

---

## 4. 開発の推奨ステップ

### Step 1: 既存コードの理解（必読ファイル）

```
backtest_limit_entry.py  ← 約定ロジックの中核 (ここを分足対応に修正)
check_signals_stop.py    ← MACD/A7/RSI2 戦略のシグナル判定 (参考実装)
check_signals_breakout.py← DON/VOL/MOM 戦略のシグナル判定 (参考実装)
scan_walkforward.py      ← WF銘柄選定パイプライン (fold設計を流用)
```

### Step 2: 分足バックテストエンジンの作成

```
backtest_intraday.py  ← 新規作成
  - fetch_intraday_data(symbol, interval="5m", days=60)
  - run_intraday_backtest(symbol, strategy, date)
    - 当日の分足データを使って1日内のP&L計算
    - MAX_HOLD = 当日15:25まで
    - stop_mode = "intraday" 固定
```

### Step 3: デイトレシグナル生成

```
check_signals_daytrade.py  ← 新規作成
  - check_signal_on_date(symbol, strategy, target_date=None)
  - STRATEGY_PARAMS: 分足ベースのパラメータ
  - build_html(): スイング版と同じ構造のHTMLレポート
```

### Step 4: WF銘柄スキャン（分足版）

```
scan_walkforward_intraday.py  ← scan_walkforward.py を参考に
  - fold設計はスイング版と同じ (12ヶ月訓練/6ヶ月テスト)
  - ただしテスト期間内の各日の「日中バックテスト」を走らせる
```

### Step 5: 検証・レポート

```
run_signals_daytrade.py  ← run_signals.py を参考に
  - 今日エントリーすべき銘柄を表示
  - BTスコア（分足版）でランキング
```

---

## 5. 既存 gobot との共存構成（推奨）

```
/home/user/gobot/
├── (既存スイング版ファイル群)   ← 変更しない
├── backtest_intraday.py          ← 新規
├── check_signals_daytrade.py     ← 新規
├── scan_walkforward_intraday.py  ← 新規
├── run_signals_daytrade.py       ← 新規
└── daytrade_watchlist.py         ← WFスキャン結果 (新規生成)
```

既存のスイング版ファイルには一切触れず、デイトレ専用ファイルを追加する。
共通で使える定数（BTスコア計算式等）は `import backtest_limit_entry` や
直接コピーで取り込む。

---

## 6. デイトレ固有の重要な注意点

### 6.1 東証の時間帯

```
08:00-09:00  気配値公開（板注文受付）
09:00        前場寄り付き
11:30        前場引け
12:30        後場寄り付き
15:30        後場引け（デイトレ決済タイムリミット）
15:25        ← タイムカットの実運用タイミング（引け30分前MOC発注）
```

### 6.2 デイトレに向く銘柄の特徴

スイング版の WATCHLIST 銘柄は「4日〜7日で目標達成する」銘柄。
デイトレには追加で以下が必要:
- **出来高が多い**: 最低でも日次100万株以上（スプレッドが小さい）
- **ボラティリティが適度**: ATR/終値 = 1.5〜3%（0.5%未満は動かない、5%超はリスク大）
- **寄り付きギャップが少ない**: ギャップがあると逆指値エントリーの計算がズレる

### 6.3 yfinance の分足データ制限の回避策

```python
# 5分足は60日分のみ取得可能 → バックテスト期間が短い
# → 解決策1: 毎日 fetch してローカルDBに蓄積
# → 解決策2: J-Quants API (日本株専門, 無料枠あり) を使う
# → 解決策3: kabuステーション API の分足取得機能

# J-Quants API の例:
# pip install jquants-api-client
# https://jpx-jquants.com/ (無料プランで分足2年分)
```

### 6.4 スイング版のホールドアウト検証の考え方をデイトレに適用

```
スイング版: 直近N日を「未来」として検証
             → ホールドアウト期間 = N日間の全取引を未使用として扱う

デイトレ版: 直近M日間の各日の「日中バックテスト」を未来として検証
             → fold設計は同じだが、1fold内の取引数がスイングより多い
             → 統計的信頼性が上がる（デイトレの利点）
```

---

## 7. 現在のスイング版の状態（2026-06-17時点）

### 保有中ポジション

| コード | 銘柄 | エントリー日 | 価格 | 数量 |
|---|---|---|---|---|
| 3563 | Ｆ＆ＬＣ | 2026-06-05 | 9,849 | 100 |
| 8050 | セイコーＧ | 2026-06-08 | 6,790 | 100 |
| 8061 | 西華産 | 2026-06-05 | 2,990 | 200 |
| 4626 | 太陽ＨＤ | 2026-06-16 | 5,366 | 100 |
| 6264 | マルマエ | 2026-06-16 | 2,547.7 | 100 |

### 直近の主要改修（参考）

- ATRペナルティ: 損切り幅>7%でBTスコア減点 (最大50%)
- 損切りモード: closeモード既定 (終値判定) → ショート特に効果大
- ⑥速度分析: 目標達成 + プラスタイムカットの保有日数分析
- 月次P&L: 1年分を表示（直近90日制限を撤廃）
- 営業日ベースのトレンド日数計算

---

## 8. 新セッションへの依頼テンプレート

新しいClaudeセッションを開く際、以下のプロンプトを使ってください:

---

**プロンプト（コピペ用）:**

```
gobot プロジェクト (/home/user/gobot) のデイトレ版を開発してください。

【参照ファイル】
- /home/user/gobot/CLAUDE.md  ← スイング版の全仕様
- /home/user/gobot/DAYTRADE_HANDOFF.md  ← デイトレ引継ぎ資料（必読）

【作業内容】
1. スイング版の方法論（WalkForward銘柄選定、BTスコア、ホールドアウトOOS）を維持
2. 分足データ対応のバックテストエンジン (backtest_intraday.py) を作成
3. デイトレ版シグナル判定 (check_signals_daytrade.py) を作成
4. まずは「前日終値ブレイク戦略」1本から実装してテストする

【重要変更点】
- ENTRY_EXPIRE: 当日中のみ (0営業日)
- MAX_HOLD: 15:25までに強制決済
- stop_mode: "intraday" 固定 (closeモードは使わない)
- データ: yfinance の 5分足 (interval="5m")
- ATR: 5分足14期間ATR (日足ATRではない)

既存のスイング版ファイルは変更せず、デイトレ専用ファイルを追加する形で実装してください。
```

---

## 9. よくある質問（FAQ）

**Q: スイング版の WATCHLIST はデイトレでも使えますか？**  
A: 参考程度には使えますが、デイトレ専用の WF スキャンで再選定するのが理想。
スイングで安定している銘柄がデイトレでも良いとは限りません（流動性や時間帯が違う）。

**Q: BTスコアの計算式は変えなくていいですか？**  
A: 計算式はそのまま流用可能です。ただし「1取引 = 1日内の取引」になるので、
分母となる取引数が増えます。閾値（BTスコア≥60）は再調整が必要かもしれません。

**Q: 並列処理の workers 数は？**  
A: スイング版は WORKERS=4 が既定。デイトレも同じで問題ありません。
分足データ取得は日足より時間がかかるため、キャッシュ戦略が重要になります。

**Q: conservative / aggressive の区別はデイトレでも必要？**  
A: 最初は conservative だけで始めることを推奨。慣れてから aggressive (より小さい目標倍率) を検討。

**Q: kabuステーション API はデイトレでも使えますか？**  
A: はい。`kabu_api.py` の `KabuClient` はデイトレでもそのまま使えます。
発注は `send_stop_buy` / `send_moc`。
ただし、逆指値の発注タイミングと取消ロジックが日中管理になるため、スクリプトの改修が必要です。
